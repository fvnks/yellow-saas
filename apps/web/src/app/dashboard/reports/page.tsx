'use client'; import { useEffect, useState } from 'react';
import { Card, CardHeader, CardTitle, CardContent, Table, TableHeader, TableBody, TableRow, TableHead, TableCell, Badge, Button, Input, Select, KPICard } from '@yellow-erp/ui';
import { Download, FileText, ShoppingCart, Package, Calculator, TrendingUp, DollarSign, Users, BarChart3, AlertTriangle, RefreshCw } from 'lucide-react';
import { getApiClient } from '@/lib/api-client';
import { ContinuousTabs } from '@/components/ui/continuous-tabs'; interface ReportSales { totalSold: number; orderCount: number; avgTicket: number; topProducts: { id: string; name: string; sku: string; units: number; total: number; percentage: number }[]; topCustomers: { id: string; name: string; tax_id: string; total: number; orders: number }[];
} interface ReportInventory { totalProducts: number; totalValue: number; lowStock: number; outOfStock: number; products: { id: string; name: string; sku: string; current_stock: number; min_stock: number; warehouse_name: string; status: string }[];
} interface ReportFinancials { totalIncome: number; totalExpenses: number; netProfit: number; totalIva: number; monthly: { month: string; income: number; expenses: number; profit: number; iva: number }[];
} export default function ReportsPage() { const [activeTab, setActiveTab] = useState('ventas'); const [dateFrom, setDateFrom] = useState(new Date(new Date().getFullYear(), 0, 1).toISOString().split('T')[0]); const [dateTo, setDateTo] = useState(new Date().toISOString().split('T')[0]); const [warehouseFilter, setWarehouseFilter] = useState('all'); const [loading, setLoading] = useState(true); const [warehouses, setWarehouses] = useState<{ value: string; label: string }[]>([]); const [sales, setSales] = useState<ReportSales | null>(null); const [inventory, setInventory] = useState<ReportInventory | null>(null); const [financials, setFinancials] = useState<ReportFinancials | null>(null); const tabs = [ { id: 'ventas', label: 'Ventas' }, { id: 'inventario', label: 'Inventario' }, { id: 'contabilidad', label: 'Contabilidad' }, ]; const loadData = async () => { setLoading(true); try { const api = getApiClient(); const [reportRes, warehousesRes] = await Promise.all([ api.getReport({ report: 'all', date_from: dateFrom, date_to: dateTo, warehouse: warehouseFilter !== 'all' ? warehouseFilter : undefined, }), api.getWarehouses({ limit: '100' }).catch(() => ({ data: [] })), ]); if (reportRes.sales) setSales(reportRes.sales); if (reportRes.inventory) setInventory(reportRes.inventory); if (reportRes.financials) setFinancials(reportRes.financials); setWarehouses((warehousesRes.data || []).map((w: any) => ({ value: w.id, label: w.name }))); } catch (err) { console.error('Failed to load reports:', err); } finally { setLoading(false); } }; useEffect(() => { loadData(); }, [dateFrom, dateTo, warehouseFilter]); const getStockBadge = (status: string) => { switch (status) { case 'out_of_stock': return <Badge variant="danger">Sin stock</Badge>; case 'low': return <Badge variant="warning">Bajo</Badge>; default: return <Badge variant="success">Normal</Badge>; } }; const exportCSV = (headers: string[], rows: string[][], filename: string) => { const csv = [headers, ...rows].map(r => r.map(v => `"${(v || '').replace(/"/g, '""')}"`).join(',')).join('\n'); const blob = new Blob(['\uFEFF' + csv], { type: 'text/csv;charset=utf-8;' }); const url = URL.createObjectURL(blob); const a = document.createElement('a'); a.href = url; a.download = `${filename}_${new Date().toISOString().slice(0, 10)}.csv`; a.click(); URL.revokeObjectURL(url); }; return ( <div className="space-y-6"> <div className="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-4"> <div> <h1 className="text-xl font-bold text-foreground">Reportes</h1> <p className="text-sm text-muted-foreground mt-1">Análisis y estadísticas del negocio</p> </div> <div className="flex items-center gap-2"> <Button variant="secondary" size="sm" onClick={loadData} disabled={loading}> <RefreshCw className={`w-4 h-4 mr-2 ${loading ? 'animate-spin' : ''}`} /> Actualizar </Button> </div> </div> <ContinuousTabs tabs={tabs} defaultActiveId={activeTab} onChange={(id) => setActiveTab(id)} /> {activeTab === 'ventas' && ( <div className="space-y-6"> <Card> <CardContent className="p-4"> <div className="flex items-center gap-4"> <Input label="Fecha desde" type="date" value={dateFrom} onChange={(e) => setDateFrom(e.target.value)} className="w-40" /> <Input label="Fecha hasta" type="date" value={dateTo} onChange={(e) => setDateTo(e.target.value)} className="w-40" /> <div className="flex-1" /> <Button variant="secondary" size="sm" onClick={() => { if (!sales) return; exportCSV( ['#', 'Producto', 'SKU', 'Unidades', 'Total', '% del total'], sales.topProducts.map((p, i) => [String(i + 1), p.name, p.sku, String(p.units), String(p.total), `${p.percentage}%`]), 'top_productos' ); }}> <Download className="w-4 h-4 mr-2" /> Exportar </Button> </div> </CardContent> </Card> <div className="grid gap-4 grid-cols-1 sm:grid-cols-2 lg:grid-cols-4"> <KPICard label="Total Vendido" value={loading ? '...' : `$${(sales?.totalSold || 0).toLocaleString('es-CL')}`} change={sales?.orderCount ? `${sales.orderCount} órdenes` : 'Sin datos'} changeType="neutral" icon={DollarSign} iconColor="emerald" /> <KPICard label="Órdenes" value={loading ? '...' : String(sales?.orderCount || 0)} change={`${sales?.orderCount || 0} en período`} changeType="neutral" icon={ShoppingCart} iconColor="blue" /> <KPICard label="Ticket Promedio" value={loading ? '...' : `$${(sales?.avgTicket || 0).toLocaleString('es-CL')}`} change="Promedio por orden" changeType="neutral" icon={BarChart3} iconColor="indigo" /> <KPICard label="Top Cliente" value={loading ? '...' : (sales?.topCustomers?.[0]?.name || 'Sin datos')} change={sales?.topCustomers?.[0] ? `$${sales.topCustomers[0].total.toLocaleString('es-CL')} en compras` : ''} changeType="neutral" icon={Users} iconColor="amber" /> </div> <Card> <CardHeader> <CardTitle>Top 10 Productos Vendidos</CardTitle> </CardHeader> <CardContent className="p-0"> <div className="overflow-x-auto"> <Table> <TableHeader> <TableRow> <TableHead>#</TableHead> <TableHead>Producto</TableHead> <TableHead>SKU</TableHead> <TableHead>Unidades</TableHead> <TableHead>Total</TableHead> <TableHead>% del total</TableHead> </TableRow> </TableHeader> <TableBody> {loading ? ( Array.from({ length: 5 }).map((_, i) => ( <TableRow key={i}> <TableCell colSpan={6}><div className="h-4 bg-muted rounded animate-pulse" /></TableCell> </TableRow> )) ) : sales?.topProducts?.length ? ( sales.topProducts.map((product, index) => ( <TableRow key={product.id}> <TableCell>{index + 1}</TableCell> <TableCell className="font-medium">{product.name}</TableCell> <TableCell className="font-mono text-xs text-muted-foreground">{product.sku}</TableCell> <TableCell>{product.units}</TableCell> <TableCell>${product.total.toLocaleString('es-CL')}</TableCell> <TableCell>{product.percentage}%</TableCell> </TableRow> )) ) : ( <TableRow><TableCell colSpan={6} className="py-8 text-center text-xs text-muted-foreground">No hay datos de ventas en este período</TableCell></TableRow> )} </TableBody> </Table> </div> </CardContent> </Card> {sales?.topCustomers?.length ? ( <Card> <CardHeader> <CardTitle>Top Clientes</CardTitle> </CardHeader> <CardContent className="p-0"> <div className="overflow-x-auto"> <Table> <TableHeader> <TableRow> <TableHead>#</TableHead> <TableHead>Cliente</TableHead> <TableHead>RUT</TableHead> <TableHead>Órdenes</TableHead> <TableHead>Total</TableHead> </TableRow> </TableHeader> <TableBody> {sales.topCustomers.map((c, i) => ( <TableRow key={c.id}> <TableCell>{i + 1}</TableCell> <TableCell className="font-medium">{c.name || 'Sin nombre'}</TableCell> <TableCell className="font-mono text-xs">{c.tax_id || '—'}</TableCell> <TableCell>{c.orders}</TableCell> <TableCell>${c.total.toLocaleString('es-CL')}</TableCell> </TableRow> ))} </TableBody> </Table> </div> </CardContent> </Card> ) : null} </div> )} {activeTab === 'inventario' && ( <div className="space-y-6"> <Card> <CardContent className="p-4"> <div className="flex items-center gap-4"> <Select label="Almacén" value={warehouseFilter} onChange={(e) => setWarehouseFilter(e.target.value)} options={[{ value: 'all', label: 'Todos los almacenes' }, ...warehouses]} className="w-64" /> <div className="flex-1" /> <Button variant="secondary" size="sm" onClick={() => { if (!inventory) return; exportCSV( ['Producto', 'SKU', 'Almacén', 'Stock Actual', 'Stock Mínimo', 'Estado'], inventory.products.map(p => [p.name, p.sku, p.warehouse_name || '—', String(p.current_stock), String(p.min_stock), p.status]), 'stock_productos' ); }}> <Download className="w-4 h-4 mr-2" /> Exportar </Button> </div> </CardContent> </Card> <div className="grid gap-4 grid-cols-1 sm:grid-cols-2 lg:grid-cols-4"> <KPICard label="Total Productos" value={loading ? '...' : String(inventory?.totalProducts || 0)} change="Activos en catálogo" changeType="neutral" icon={Package} iconColor="indigo" /> <KPICard label="Valor Inventario" value={loading ? '...' : `$${(inventory?.totalValue || 0).toLocaleString('es-CL')}`} change="Costo de reposición" changeType="neutral" icon={DollarSign} iconColor="emerald" /> <KPICard label="Stock Bajo" value={loading ? '...' : String(inventory?.lowStock || 0)} change="Requiere reabastecimiento" changeType="negative" icon={AlertTriangle} iconColor="amber" /> <KPICard label="Sin Stock" value={loading ? '...' : String(inventory?.outOfStock || 0)} change="Crítico - sin disponibilidad" changeType="negative" icon={AlertTriangle} iconColor="rose" /> </div> <Card> <CardHeader> <CardTitle>Stock por Producto</CardTitle> </CardHeader> <CardContent className="p-0"> <div className="overflow-x-auto"> <Table> <TableHeader> <TableRow> <TableHead>Producto</TableHead> <TableHead>SKU</TableHead> <TableHead>Almacén</TableHead> <TableHead>Stock Actual</TableHead> <TableHead>Stock Mínimo</TableHead> <TableHead>Estado</TableHead> </TableRow> </TableHeader> <TableBody> {loading ? ( Array.from({ length: 5 }).map((_, i) => ( <TableRow key={i}> <TableCell colSpan={6}><div className="h-4 bg-muted rounded animate-pulse" /></TableCell> </TableRow> )) ) : inventory?.products?.length ? ( inventory.products.map(product => ( <TableRow key={product.id}> <TableCell className="font-medium">{product.name}</TableCell> <TableCell className="font-mono text-xs text-muted-foreground">{product.sku}</TableCell> <TableCell>{product.warehouse_name || '—'}</TableCell> <TableCell className={product.current_stock === 0 ? 'text-rose-600 font-medium' : ''}>{product.current_stock}</TableCell> <TableCell>{product.min_stock}</TableCell> <TableCell>{getStockBadge(product.status)}</TableCell> </TableRow> )) ) : ( <TableRow><TableCell colSpan={6} className="py-8 text-center text-xs text-muted-foreground">No hay productos en inventario</TableCell></TableRow> )} </TableBody> </Table> </div> </CardContent> </Card> </div> )} {activeTab === 'contabilidad' && ( <div className="space-y-6"> <Card> <CardContent className="p-4"> <div className="flex items-center gap-4"> <Input label="Fecha desde" type="date" value={dateFrom} onChange={(e) => setDateFrom(e.target.value)} className="w-40" /> <Input label="Fecha hasta" type="date" value={dateTo} onChange={(e) => setDateTo(e.target.value)} className="w-40" /> <div className="flex-1" /> <Button variant="secondary" size="sm" onClick={() => { if (!financials) return; exportCSV( ['Mes', 'Ingresos', 'Gastos', 'Utilidad', 'IVA'], financials.monthly.map(m => [m.month, String(m.income), String(m.expenses), String(m.profit), String(m.iva)]), 'resumen_financiero' ); }}> <Download className="w-4 h-4 mr-2" /> Exportar </Button> </div> </CardContent> </Card> <div className="grid gap-4 grid-cols-1 sm:grid-cols-2 lg:grid-cols-4"> <KPICard label="Ingresos" value={loading ? '...' : `$${(financials?.totalIncome || 0).toLocaleString('es-CL')}`} change={`Facturas emitidas`} changeType="positive" icon={TrendingUp} iconColor="emerald" /> <KPICard label="Gastos" value={loading ? '...' : `$${(financials?.totalExpenses || 0).toLocaleString('es-CL')}`} change={`Órdenes de compra`} changeType="negative" icon={DollarSign} iconColor="rose" /> <KPICard label="Utilidad Neta" value={loading ? '...' : `$${(financials?.netProfit || 0).toLocaleString('es-CL')}`} change={financials?.netProfit && financials.netProfit > 0 ? 'Positivo' : 'Revisar'} changeType={financials?.netProfit && financials.netProfit > 0 ? 'positive' : 'negative'} icon={TrendingUp} iconColor="blue" /> <KPICard label="IVA Cobrado" value={loading ? '...' : `$${(financials?.totalIva || 0).toLocaleString('es-CL')}`} change="Período fiscal" changeType="neutral" icon={Calculator} iconColor="amber" /> </div> <Card> <CardHeader> <CardTitle>Resumen Mensual</CardTitle> </CardHeader> <CardContent className="p-0"> <div className="overflow-x-auto"> <Table> <TableHeader> <TableRow> <TableHead>Mes</TableHead> <TableHead>Ingresos</TableHead> <TableHead>Gastos</TableHead> <TableHead>Utilidad</TableHead> <TableHead>IVA</TableHead> </TableRow> </TableHeader> <TableBody> {loading ? ( Array.from({ length: 3 }).map((_, i) => ( <TableRow key={i}> <TableCell colSpan={5}><div className="h-4 bg-muted rounded animate-pulse" /></TableCell> </TableRow> )) ) : financials?.monthly?.length ? ( financials.monthly.map(month => ( <TableRow key={month.month}> <TableCell className="font-medium">{month.month}</TableCell> <TableCell>${month.income.toLocaleString('es-CL')}</TableCell> <TableCell>${month.expenses.toLocaleString('es-CL')}</TableCell> <TableCell> <span className={month.profit >= 0 ? 'text-emerald-600 font-medium' : 'text-rose-600 font-medium'}> ${month.profit.toLocaleString('es-CL')} </span> </TableCell> <TableCell>${month.iva.toLocaleString('es-CL')}</TableCell> </TableRow> )) ) : ( <TableRow><TableCell colSpan={5} className="py-8 text-center text-xs text-muted-foreground">No hay datos financieros en este período</TableCell></TableRow> )} </TableBody> </Table> </div> </CardContent> </Card> </div> )} </div> );
}
