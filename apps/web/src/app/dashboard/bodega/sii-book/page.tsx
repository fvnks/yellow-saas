'use client'; import { useState, useEffect } from 'react'; import { ArrowLeft, BookOpen, Download } from 'lucide-react'; import Link from 'next/link'; import { getApiClient } from '@/lib/api-client'; export default function SIIBookPage() { const [entries, setEntries] = useState<any[]>([]); const [loading, setLoading] = useState(true); const [periodStart, setPeriodStart] = useState(''); const [periodEnd, setPeriodEnd] = useState(''); useEffect(() => { loadEntries(); }, []); const loadEntries = async () => { setLoading(true); try { const api = getApiClient(); const params: Record<string, string> = { limit: '500' }; if (periodStart) params.period_start = periodStart; if (periodEnd) params.period_end = periodEnd; const res = await api.getSIIInventoryBook(params); setEntries(res.data || []); } catch (err) { console.error(err); } setLoading(false); }; const totalOpeningValue = entries.reduce((s, e) => s + (parseFloat(e.opening_value) || 0), 0); const totalEntriesValue = entries.reduce((s, e) => s + (parseFloat(e.entries_value) || 0), 0); const totalExitsValue = entries.reduce((s, e) => s + (parseFloat(e.exits_value) || 0), 0); const totalClosingValue = entries.reduce((s, e) => s + (parseFloat(e.closing_value) || 0), 0); return ( <div className="space-y-6"> <div className="flex items-center gap-4"> <Link href="/dashboard/bodega" className="p-2 hover:bg-muted rounded-lg transition-colors"> <ArrowLeft className="w-5 h-5 text-foreground" /> </Link> <div className="flex-1"> <h1 className="text-xl font-bold text-foreground">Libro Inventario SII</h1> <p className="text-sm text-muted-foreground mt-1">Control de inventario para SII (Servicio de Impuestos Internos)</p> </div> </div> <div className="bg-card border border-border rounded-xl shadow-sm p-4 "> <div className="flex items-center gap-4"> <div className="space-y-1"> <label className="block text-xs font-medium text-foreground">Periodo Inicio</label> <input type="date" value={periodStart} onChange={e => setPeriodStart(e.target.value)} className="bg-muted border border-border rounded-lg px-3 py-2 text-sm text-foreground focus:outline-none focus:ring-2 focus:ring-primary/20 focus:border-transparent" /> </div> <div className="space-y-1"> <label className="block text-xs font-medium text-foreground">Periodo Fin</label> <input type="date" value={periodEnd} onChange={e => setPeriodEnd(e.target.value)} className="bg-muted border border-border rounded-lg px-3 py-2 text-sm text-foreground focus:outline-none focus:ring-2 focus:ring-primary/20 focus:border-transparent" /> </div> <button onClick={loadEntries} className="bg-primary hover:bg-primary/90 text-white px-4 py-2 rounded-lg text-sm font-medium transition-colors mt-5"> Filtrar </button> </div> </div> <div className="grid gap-4 md:grid-cols-4"> <div className="bg-card border border-border rounded-xl shadow-sm p-4 "> <p className="text-[9px] font-semibold text-muted-foreground uppercase tracking-wider">Saldo Inicial</p> <p className="text-lg font-bold text-foreground mt-1">${totalOpeningValue.toLocaleString('es-CL')}</p> </div> <div className="bg-card border border-border rounded-xl shadow-sm p-4 "> <p className="text-[9px] font-semibold text-muted-foreground uppercase tracking-wider">Entradas</p> <p className="text-lg font-bold text-emerald-600 mt-1">${totalEntriesValue.toLocaleString('es-CL')}</p> </div> <div className="bg-card border border-border rounded-xl shadow-sm p-4 "> <p className="text-[9px] font-semibold text-muted-foreground uppercase tracking-wider">Salidas</p> <p className="text-lg font-bold text-rose-600 mt-1">${totalExitsValue.toLocaleString('es-CL')}</p> </div> <div className="bg-card border border-border rounded-xl shadow-sm p-4 "> <p className="text-[9px] font-semibold text-muted-foreground uppercase tracking-wider">Saldo Final</p> <p className="text-lg font-bold text-foreground mt-1">${totalClosingValue.toLocaleString('es-CL')}</p> </div> </div> <div className="bg-card border border-border rounded-xl shadow-sm "> <div className="overflow-x-auto"> <table className="w-full"> <thead> <tr className="border-b border-border"> <th className="text-left px-4 py-3 text-[9px] font-semibold text-muted-foreground uppercase tracking-wider">Producto</th> <th className="text-left px-4 py-3 text-[9px] font-semibold text-muted-foreground uppercase tracking-wider">Bodega</th> <th className="text-center px-4 py-3 text-[9px] font-semibold text-muted-foreground uppercase tracking-wider">Metodo</th> <th className="text-right px-4 py-3 text-[9px] font-semibold text-muted-foreground uppercase tracking-wider">Apertura Qty</th> <th className="text-right px-4 py-3 text-[9px] font-semibold text-muted-foreground uppercase tracking-wider">Apertura Valor</th> <th className="text-right px-4 py-3 text-[9px] font-semibold text-muted-foreground uppercase tracking-wider">Entradas Qty</th> <th className="text-right px-4 py-3 text-[9px] font-semibold text-muted-foreground uppercase tracking-wider">Entradas Valor</th> <th className="text-right px-4 py-3 text-[9px] font-semibold text-muted-foreground uppercase tracking-wider">Salidas Qty</th> <th className="text-right px-4 py-3 text-[9px] font-semibold text-muted-foreground uppercase tracking-wider">Salidas Valor</th> <th className="text-right px-4 py-3 text-[9px] font-semibold text-muted-foreground uppercase tracking-wider">Cierre Qty</th> <th className="text-right px-4 py-3 text-[9px] font-semibold text-muted-foreground uppercase tracking-wider">Cierre Valor</th> </tr> </thead> <tbody> {loading ? ( <tr><td colSpan={11} className="px-4 py-12 text-center text-sm text-muted-foreground">Cargando...</td></tr> ) : entries.length === 0 ? ( <tr><td colSpan={11} className="px-4 py-12 text-center"> <BookOpen className="w-12 h-12 text-foreground mx-auto mb-3" /> <p className="text-sm text-muted-foreground">No hay registros en el libro de inventario</p> </td></tr> ) : entries.map((e, i) => ( <tr key={i} className="border-b border-border hover:bg-muted transition-colors"> <td className="px-4 py-3 text-xs font-medium text-foreground">{e.product_name || '—'}</td> <td className="px-4 py-3 text-xs text-foreground">{e.warehouse_name || '—'}</td> <td className="px-4 py-3 text-xs text-center text-foreground">{e.cost_method || 'FIFO'}</td> <td className="px-4 py-3 text-xs text-right text-foreground">{e.opening_qty || 0}</td> <td className="px-4 py-3 text-xs text-right text-foreground">${Number(e.opening_value || 0).toLocaleString('es-CL')}</td> <td className="px-4 py-3 text-xs text-right text-foreground">{e.entries_qty || 0}</td> <td className="px-4 py-3 text-xs text-right text-emerald-600">${Number(e.entries_value || 0).toLocaleString('es-CL')}</td> <td className="px-4 py-3 text-xs text-right text-foreground">{e.exits_qty || 0}</td> <td className="px-4 py-3 text-xs text-right text-rose-600">${Number(e.exits_value || 0).toLocaleString('es-CL')}</td> <td className="px-4 py-3 text-xs text-right font-medium text-foreground">{e.closing_qty || 0}</td> <td className="px-4 py-3 text-xs text-right font-bold text-foreground">${Number(e.closing_value || 0).toLocaleString('es-CL')}</td> </tr> ))} </tbody> </table> </div> </div> </div> ); } 