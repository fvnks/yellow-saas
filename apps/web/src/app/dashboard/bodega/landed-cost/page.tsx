'use client'; import { useState, useEffect } from 'react'; import { ArrowLeft, Plus, Search, Eye, Trash2, Package, DollarSign, Scale } from 'lucide-react'; import Link from 'next/link'; import { getApiClient } from '@/lib/api-client'; const costTypeConfig: Record<string, { label: string; color: string }> = { freight: { label: 'Flete', color: 'bg-blue-100 text-blue-700' }, insurance: { label: 'Seguro', color: 'bg-blue-50 text-primary' }, customs_duty: { label: 'Arancel', color: 'bg-amber-100 text-amber-700' }, handling: { label: 'Manipuleo', color: 'bg-blue-50 text-blue-700' }, other: { label: 'Otro', color: 'bg-muted text-foreground' }, }; const statusColors: Record<string, string> = { draft: 'bg-muted text-foreground', approved: 'bg-emerald-100 text-emerald-700', allocated: 'bg-blue-100 text-blue-700', }; export default function LandedCostPage() { const [allocations, setAllocations] = useState<any[]>([]); const [loading, setLoading] = useState(true); useEffect(() => { loadAllocations(); }, []); const loadAllocations = async () => { setLoading(true); try { const api = getApiClient(); const res = await api.getLandedCostAllocations({ limit: '100' }); setAllocations(res.data || []); } catch (err) { console.error(err); } setLoading(false); }; const handleDelete = async (id: string) => { if (!confirm('Eliminar esta asignacion?')) return; try { const api = getApiClient(); await api.deleteLandedCostAllocation(id); loadAllocations(); } catch (err) { console.error(err); } }; return ( <div className="space-y-6"> <div className="flex items-center gap-4"> <Link href="/dashboard/bodega" className="p-2 hover:bg-muted rounded-lg transition-colors"> <ArrowLeft className="w-5 h-5 text-foreground" /> </Link> <div className="flex-1"> <h1 className="text-xl font-bold text-foreground">Costos Aterrizados</h1> <p className="text-sm text-muted-foreground mt-1">Fletes, seguros, aranceles y otros costos de importacion</p> </div> </div> <div className="bg-card border border-border rounded-xl shadow-sm "> <div className="overflow-x-auto"> <table className="w-full"> <thead> <tr className="border-b border-border"> <th className="text-left px-4 py-3 text-[9px] font-semibold text-muted-foreground uppercase tracking-wider">ID</th> <th className="text-left px-4 py-3 text-[9px] font-semibold text-muted-foreground uppercase tracking-wider">Orden Compra</th> <th className="text-left px-4 py-3 text-[9px] font-semibold text-muted-foreground uppercase tracking-wider">Tipo</th> <th className="text-right px-4 py-3 text-[9px] font-semibold text-muted-foreground uppercase tracking-wider">Monto</th> <th className="text-center px-4 py-3 text-[9px] font-semibold text-muted-foreground uppercase tracking-wider">Metodo</th> <th className="text-center px-4 py-3 text-[9px] font-semibold text-muted-foreground uppercase tracking-wider">Estado</th> <th className="text-center px-4 py-3 text-[9px] font-semibold text-muted-foreground uppercase tracking-wider">Acciones</th> </tr> </thead> <tbody> {loading ? ( <tr><td colSpan={7} className="px-4 py-12 text-center text-sm text-muted-foreground">Cargando...</td></tr> ) : allocations.length === 0 ? ( <tr><td colSpan={7} className="px-4 py-12 text-center"> <Package className="w-12 h-12 text-foreground mx-auto mb-3" /> <p className="text-sm text-muted-foreground">No hay costos aterrizados registrados</p> </td></tr> ) : allocations.map(a => ( <tr key={a.id} className="border-b border-border hover:bg-muted transition-colors"> <td className="px-4 py-3 text-xs font-mono text-foreground">{a.id?.slice(0, 8)}...</td> <td className="px-4 py-3 text-xs text-foreground">{a.purchase_order_number || '—'}</td> <td className="px-4 py-3"> <span className={`inline-flex items-center px-2 py-0.5 rounded-full text-[9px] font-semibold ${costTypeConfig[a.cost_type]?.color || 'bg-muted text-foreground'}`}> {costTypeConfig[a.cost_type]?.label || a.cost_type} </span> </td> <td className="px-4 py-3 text-xs text-right font-medium text-foreground">${Number(a.amount || 0).toLocaleString('es-CL')}</td> <td className="px-4 py-3 text-xs text-center text-foreground capitalize">{a.allocation_method || 'value'}</td> <td className="px-4 py-3 text-center"> <span className={`inline-flex items-center px-2 py-0.5 rounded-full text-[9px] font-semibold ${statusColors[a.status] || 'bg-muted text-foreground'}`}> {a.status || 'draft'} </span> </td> <td className="px-4 py-3 text-center"> <div className="flex items-center justify-center gap-1"> <button onClick={() => handleDelete(a.id)} className="p-1.5 hover:bg-red-50 rounded-lg transition-colors"> <Trash2 className="w-3.5 h-3.5 text-red-500" /> </button> </div> </td> </tr> ))} </tbody> </table> </div> </div> </div> ); } 