'use client'; import { useState, useEffect } from 'react'; import { Card, CardHeader, CardTitle, CardContent, Button, Input, Select } from '@yellow-erp/ui'; import { ArrowLeft, Save, Warehouse } from 'lucide-react'; import Link from 'next/link'; import { useRouter } from 'next/navigation'; import { getApiClient } from '@/lib/api-client'; const regions = [ { value: 'Arica y Parinacota', label: 'XV - Arica y Parinacota' }, { value: 'Tarapacá', label: 'I - Tarapacá' }, { value: 'Antofagasta', label: 'II - Antofagasta' }, { value: 'Atacama', label: 'III - Atacama' }, { value: 'Coquimbo', label: 'IV - Coquimbo' }, { value: 'Valparaíso', label: 'V - Valparaíso' }, { value: 'Metropolitana', label: 'RM - Metropolitana' }, { value: 'O\'Higgins', label: 'VI - O\'Higgins' }, { value: 'Maule', label: 'VII - Maule' }, { value: 'Ñuble', label: 'XVI - Ñuble' }, { value: 'Biobío', label: 'VIII - Biobío' }, { value: 'La Araucanía', label: 'IX - La Araucanía' }, { value: 'Los Ríos', label: 'XIV - Los Ríos' }, { value: 'Los Lagos', label: 'X - Los Lagos' }, { value: 'Aysén', label: 'XI - Aysén' }, { value: 'Magallanes', label: 'XII - Magallanes' }, ]; export default function EditWarehousePage({ params }: { params: { id: string } }) { const router = useRouter(); const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false); const [error, setError] = useState(''); const [name, setName] = useState(''); const [code, setCode] = useState(''); const [address, setAddress] = useState(''); const [city, setCity] = useState(''); const [region, setRegion] = useState(''); const [country, setCountry] = useState('CL'); const [phone, setPhone] = useState(''); const [email, setEmail] = useState(''); const [isDefault, setIsDefault] = useState(false); const [isActive, setIsActive] = useState(true); useEffect(() => { const api = getApiClient(); api.getWarehouse(params.id) .then((data: any) => { setName(data.name || ''); setCode(data.code || ''); setAddress(data.address || ''); setCity(data.city || ''); setRegion(data.region || ''); setCountry(data.country || 'CL'); setPhone(data.phone || ''); setEmail(data.email || ''); setIsDefault(data.is_default || false); setIsActive(data.is_active !== false); setLoading(false); }) .catch(() => { setError('Error al cargar la bodega'); setLoading(false); }); }, [params.id]); const handleSave = async () => { if (!name.trim() || !code.trim()) { setError('Nombre y código son obligatorios'); return; } setSaving(true); setError(''); try { const api = getApiClient(); await api.updateWarehouse(params.id, { name: name.trim(), code: code.trim(), address: address.trim() || undefined, city: city.trim() || undefined, region: region || undefined, country, phone: phone.trim() || undefined, email: email.trim() || undefined, is_default: isDefault, is_active: isActive, }); router.push('/dashboard/warehouses'); } catch (err) { setError(err instanceof Error ? err.message : 'Error al guardar los cambios'); setSaving(false); } }; if (loading) { return ( <div className="space-y-6"> <div className="flex items-center gap-4"> <div className="w-9 h-9 bg-muted rounded-lg animate-pulse" /> <div className="h-6 w-48 bg-muted rounded animate-pulse" /> </div> <div className="grid gap-6 lg:grid-cols-3"> <div className="lg:col-span-2"> <div className="h-64 bg-muted rounded-xl animate-pulse" /> </div> <div className="h-48 bg-muted rounded-xl animate-pulse" /> </div> </div> ); } return ( <div className="space-y-6"> <div className="flex flex-col sm:flex-row sm:items-center gap-4"> <Link href="/dashboard/warehouses" className="p-2 text-muted-foreground hover:text-foreground hover:bg-muted rounded-lg transition-colors"> <ArrowLeft className="w-5 h-5" /> </Link> <div className="flex-1"> <h1 className="text-xl font-bold text-foreground">Editar Bodega</h1> <p className="text-sm text-muted-foreground mt-1">Modificar información de la bodega</p> </div> <Button onClick={handleSave} disabled={saving} className="w-full sm:w-auto justify-center"> <Save className="w-4 h-4 mr-2" /> {saving ? 'Guardando...' : 'Guardar Cambios'} </Button> </div> {error && ( <div className="p-3 bg-rose-50 border border-rose-200 rounded-lg text-sm text-rose-700"> {error} </div> )} <div className="grid gap-6 lg:grid-cols-3"> <div className="lg:col-span-2"> <Card> <CardHeader> <CardTitle>Información General</CardTitle> </CardHeader> <CardContent className="space-y-4"> <div className="grid grid-cols-1 md:grid-cols-2 gap-4"> <Input label="Nombre *" value={name} onChange={(e) => setName(e.target.value)} placeholder="Ej: Bodega Central" /> <Input label="Código *" value={code} onChange={(e) => setCode(e.target.value)} placeholder="Ej: BOD-001" /> </div> <Input label="Dirección" value={address} onChange={(e) => setAddress(e.target.value)} placeholder="Av. Ejemplo 1234" /> <div className="grid grid-cols-1 md:grid-cols-2 gap-4"> <Input label="Ciudad" value={city} onChange={(e) => setCity(e.target.value)} placeholder="Santiago" /> <Select label="Región" value={region} onChange={(e) => setRegion(e.target.value)} options={[{ value: '', label: 'Seleccionar región...' }, ...regions]} /> </div> <div className="grid grid-cols-1 md:grid-cols-2 gap-4"> <Input label="Teléfono" value={phone} onChange={(e) => setPhone(e.target.value)} placeholder="+56 9 1234 5678" /> <Input label="Email" type="email" value={email} onChange={(e) => setEmail(e.target.value)} placeholder="bodega@empresa.cl" /> </div> </CardContent> </Card> </div> <div className="space-y-6"> <Card> <CardHeader> <CardTitle>Configuración</CardTitle> </CardHeader> <CardContent className="space-y-4"> <div className="flex items-center justify-between"> <div> <label className="text-sm font-medium text-foreground">Bodega por Defecto</label> <p className="text-xs text-muted-foreground">Se usará automáticamente en nuevas órdenes</p> </div> <button onClick={() => setIsDefault(!isDefault)} className={`relative inline-flex h-6 w-11 items-center rounded-full transition-colors ${isDefault ? 'bg-primary' : 'bg-muted'}`} > <span className={`inline-block h-4 w-4 transform rounded-full bg-white transition-transform ${isDefault ? 'translate-x-6' : 'translate-x-1'}`} /> </button> </div> <div className="flex items-center justify-between"> <div> <label className="text-sm font-medium text-foreground">Activa</label> <p className="text-xs text-muted-foreground">Bodega visible en el sistema</p> </div> <button onClick={() => setIsActive(!isActive)} className={`relative inline-flex h-6 w-11 items-center rounded-full transition-colors ${isActive ? 'bg-primary' : 'bg-muted'}`} > <span className={`inline-block h-4 w-4 transform rounded-full bg-white transition-transform ${isActive ? 'translate-x-6' : 'translate-x-1'}`} /> </button> </div> </CardContent> </Card> <Card className="sticky top-24"> <CardHeader> <CardTitle>Resumen</CardTitle> </CardHeader> <CardContent className="space-y-3 text-sm"> <div className="flex items-center gap-3"> <div className="w-8 h-8 bg-blue-50 rounded-lg flex items-center justify-center"> <Warehouse className="w-4 h-4 text-primary" /> </div> <div> <p className="text-[9px] font-semibold text-muted-foreground uppercase tracking-wider">Código</p> <p className="font-medium text-foreground">{code || '—'}</p> </div> </div> <div className="flex justify-between"> <span className="text-muted-foreground">Nombre</span> <span className="font-medium">{name || '—'}</span> </div> <div className="flex justify-between"> <span className="text-muted-foreground">Ciudad</span> <span className="font-medium">{city || '—'}</span> </div> <div className="flex justify-between"> <span className="text-muted-foreground">Región</span> <span className="font-medium">{region || '—'}</span> </div> <div className="flex justify-between"> <span className="text-muted-foreground">Estado</span> <span className={`font-medium ${isActive ? 'text-emerald-600' : 'text-muted-foreground'}`}>{isActive ? 'Activa' : 'Inactiva'}</span> </div> </CardContent> </Card> </div> </div> </div> ); } 