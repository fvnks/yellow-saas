'use client'; import { useState, useEffect } from 'react'; import { AlertTriangle, Bell, X, CheckCircle2, TrendingUp } from 'lucide-react'; interface BudgetAlert { id: string; project_id: string; threshold_percent: number; alert_type: 'warning' | 'danger' | 'critical'; message: string | null; is_read: boolean; triggered_at: string; } interface BudgetAlertsProps { projectId: string; budget: number; costs: any[]; expenses: any[]; } const alertStyles: Record<string, { bg: string; border: string; text: string; icon: string }> = { warning: { bg: 'bg-amber-50', border: 'border-amber-200', text: 'text-amber-700', icon: 'text-amber-500' }, danger: { bg: 'bg-orange-50', border: 'border-orange-200', text: 'text-orange-700', icon: 'text-orange-500' }, critical: { bg: 'bg-red-50', border: 'border-red-200', text: 'text-red-700', icon: 'text-red-500' }, }; export default function BudgetAlerts({ projectId, budget, costs, expenses }: BudgetAlertsProps) { const [alerts, setAlerts] = useState<BudgetAlert[]>([]); const [loading, setLoading] = useState(true); const [showConfig, setShowConfig] = useState(false); const [newThreshold, setNewThreshold] = useState('75'); const totalUsed = costs.reduce((sum, c) => sum + (parseFloat(c.amount) || 0), 0) + expenses.reduce((sum, e) => sum + (parseFloat(e.amount) || 0), 0); const percentUsed = budget > 0 ? Math.round((totalUsed / budget) * 100) : 0; useEffect(() => { loadAlerts(); }, [projectId]); const loadAlerts = async () => { try { const companyId = localStorage.getItem('company_id'); const res = await fetch(`/api/companies/${companyId}/projects/${projectId}/budget-alerts`); if (res.ok) { const json = await res.json(); setAlerts(Array.isArray(json.data) ? json.data : []); } } catch (e) { console.error('Error loading alerts:', e); } finally { setLoading(false); } }; const createAlert = async () => { try { const companyId = localStorage.getItem('company_id'); const res = await fetch(`/api/companies/${companyId}/projects/${projectId}/budget-alerts`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ threshold_percent: parseInt(newThreshold) }), }); if (res.ok) { loadAlerts(); setShowConfig(false); } } catch (e) { console.error('Error creating alert:', e); } }; const markAsRead = async (alertId: string) => { try { const companyId = localStorage.getItem('company_id'); await fetch(`/api/companies/${companyId}/projects/${projectId}/budget-alerts/${alertId}`, { method: 'PATCH', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ is_read: true }), }); setAlerts(prev => prev.map(a => a.id === alertId ? { ...a, is_read: true } : a)); } catch (e) { console.error('Error marking alert:', e); } }; const unreadAlerts = alerts.filter(a => !a.is_read); const hasUnread = unreadAlerts.length > 0; if (loading) return null; return ( <div className="space-y-3"> {/* Budget Progress Bar */} <div className="bg-card border border-border rounded-xl shadow-sm p-4 "> <div className="flex items-center justify-between mb-3"> <div className="flex items-center gap-2"> <TrendingUp className="w-4 h-4 text-muted-foreground" /> <span className="text-sm font-semibold text-foreground">Uso de Presupuesto</span> </div> <div className="flex items-center gap-2"> <span className={`text-sm font-bold ${percentUsed > 90 ? 'text-red-600' : percentUsed > 75 ? 'text-orange-600' : 'text-foreground'}`}> {percentUsed}% </span> <button onClick={() => setShowConfig(!showConfig)} className="p-1.5 hover:bg-muted rounded-lg transition-colors"> <Bell className={`w-3.5 h-3.5 ${hasUnread ? 'text-amber-500' : 'text-muted-foreground'}`} /> </button> </div> </div> <div className="w-full bg-muted rounded-full h-3 overflow-hidden"> <div className={`h-full rounded-full transition-all ${ percentUsed > 90 ? 'bg-red-500' : percentUsed > 75 ? 'bg-orange-500' : percentUsed > 50 ? 'bg-amber-500' : 'bg-emerald-500' }`} style={{ width: `${Math.min(percentUsed, 100)}%` }} /> </div> <div className="flex justify-between mt-2 text-xs text-muted-foreground"> <span>${totalUsed.toLocaleString('es-CL')} gastado</span> <span>${budget.toLocaleString('es-CL')} total</span> </div> {/* Threshold markers */} <div className="relative mt-1 h-2"> {[50, 75, 90].map(threshold => ( <div key={threshold} className="absolute top-0 w-px h-2 bg-muted" style={{ left: `${threshold}%` }} title={`${threshold}%`} /> ))} </div> </div> {/* Alert Config Panel */} {showConfig && ( <div className="bg-card border border-border rounded-xl shadow-sm p-4 "> <div className="flex items-center justify-between mb-3"> <h4 className="text-xs font-semibold text-foreground">Configurar Alertas</h4> <button onClick={() => setShowConfig(false)} className="p-1 hover:bg-muted rounded"> <X className="w-3 h-3 text-muted-foreground" /> </button> </div> <div className="flex items-center gap-2 mb-3"> <span className="text-xs text-foreground">Alertar al</span> <select value={newThreshold} onChange={e => setNewThreshold(e.target.value)} className="bg-muted border border-border rounded-lg px-2 py-1 text-xs focus:outline-none focus:ring-1 focus:ring-primary/20"> <option value="50">50%</option> <option value="60">60%</option> <option value="75">75%</option> <option value="80">80%</option> <option value="90">90%</option> <option value="95">95%</option> <option value="100">100%</option> </select> <span className="text-xs text-foreground">del presupuesto</span> <button onClick={createAlert} className="bg-primary hover:bg-primary/90 text-white px-3 py-1 rounded-lg text-xs font-medium transition-colors"> Crear </button> </div> {alerts.length > 0 && ( <div className="space-y-1"> <span className="text-[9px] font-semibold text-muted-foreground uppercase tracking-wider">Alertas Configuradas</span> {alerts.map(alert => { const style = alertStyles[alert.alert_type]; return ( <div key={alert.id} className={`flex items-center justify-between p-2 rounded-lg ${alert.is_read ? 'bg-muted' : style.bg} ${alert.is_read ? '' : style.border + ' border'}`}> <div className="flex items-center gap-2"> {alert.is_read ? ( <CheckCircle2 className="w-3.5 h-3.5 text-muted-foreground" /> ) : ( <AlertTriangle className={`w-3.5 h-3.5 ${style.icon}`} /> )} <span className={`text-xs ${alert.is_read ? 'text-muted-foreground' : style.text}`}> {alert.threshold_percent}% - {alert.message || `Alerta al ${alert.threshold_percent}%`} </span> </div> {!alert.is_read && ( <button onClick={() => markAsRead(alert.id)} className="text-[9px] text-muted-foreground hover:text-foreground underline"> Marcar leido </button> )} </div> ); })} </div> )} </div> )} {/* Active unread alerts (banner) */} {unreadAlerts.length > 0 && !showConfig && ( <div className={`p-3 rounded-xl border ${alertStyles[unreadAlerts[0].alert_type].bg} ${alertStyles[unreadAlerts[0].alert_type].border}`}> <div className="flex items-center gap-2"> <AlertTriangle className={`w-4 h-4 ${alertStyles[unreadAlerts[0].alert_type].icon}`} /> <span className={`text-xs font-medium ${alertStyles[unreadAlerts[0].alert_type].text}`}> Presupuesto al {percentUsed}% — {unreadAlerts.length} alerta{unreadAlerts.length > 1 ? 's' : ''} activa{unreadAlerts.length > 1 ? 's' : ''} </span> <button onClick={() => setShowConfig(true)} className="ml-auto text-[9px] underline text-muted-foreground">Ver</button> </div> </div> )} </div> ); } 