'use client'; import { useState, useRef } from 'react'; import { useRouter } from 'next/navigation'; import { ArrowLeft, Upload, FileSpreadsheet, CheckCircle, AlertCircle, Download } from 'lucide-react'; import { getApiClient } from '@/lib/api-client'; interface ImportResult { imported: number; skipped: number; errors?: string[]; } export default function ImportProductsPage() { const router = useRouter(); const fileInputRef = useRef<HTMLInputElement>(null); const [file, setFile] = useState<File | null>(null); const [preview, setPreview] = useState<any[]>([]); const [importing, setImporting] = useState(false); const [result, setResult] = useState<ImportResult | null>(null); const [error, setError] = useState<string | null>(null); const handleFileChange = (e: React.ChangeEvent<HTMLInputElement>) => { const selected = e.target.files?.[0]; if (!selected) return; if (!selected.name.endsWith('.csv')) { setError('Solo se aceptan archivos CSV'); return; } setFile(selected); setResult(null); setError(null); const reader = new FileReader(); reader.onload = (event) => { const text = event.target?.result as string; const lines = text.split('\n').filter(l => l.trim()); if (lines.length < 2) { setError('El archivo debe tener encabezados y al menos una fila de datos'); return; } const headers = lines[0].split(',').map(h => h.trim().toLowerCase()); const requiredHeaders = ['name', 'sku']; const hasRequired = requiredHeaders.every(h => headers.includes(h)); if (!hasRequired) { setError('El archivo debe tener columnas "name" y "sku"'); return; } const rows = lines.slice(1).map(line => { const values = line.split(',').map(v => v.trim()); const row: any = {}; headers.forEach((h, i) => { row[h] = values[i] || ''; }); return row; }); setPreview(rows.slice(0, 5)); }; reader.readAsText(selected); }; const handleImport = async () => { if (!file) return; setImporting(true); setError(null); try { const reader = new FileReader(); reader.onload = async (event) => { const text = event.target?.result as string; const lines = text.split('\n').filter(l => l.trim()); const headers = lines[0].split(',').map(h => h.trim().toLowerCase()); const rows = lines.slice(1).map(line => { const values = line.split(',').map(v => v.trim()); const row: any = {}; headers.forEach((h, i) => { row[h] = values[i] || ''; }); return row; }); const api = getApiClient(); const res = await fetch(`/api/companies/${(api as any).companyId}/products/import`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ rows }), }); const data = await res.json(); if (data.success) { setResult(data.data); } else { setError(data.error?.message || 'Error al importar'); } setImporting(false); }; reader.readAsText(file); } catch { setError('Error al procesar el archivo'); setImporting(false); } }; const downloadTemplate = () => { const headers = ['name', 'sku', 'description', 'type', 'unit_of_measure', 'cost_price', 'sale_price', 'min_stock', 'max_stock', 'barcode', 'category', 'tax_id']; const example = [' Producto Ejemplo', 'SKU-001', 'Descripcion del producto', 'product', 'unit', '10000', '15000', '5', '100', '7800000000001', 'Categoria', 'IVA']; const csv = [headers.join(','), example.join(',')].join('\n'); const blob = new Blob([csv], { type: 'text/csv' }); const url = URL.createObjectURL(blob); const a = document.createElement('a'); a.href = url; a.download = 'template_productos.csv'; a.click(); URL.revokeObjectURL(url); }; return ( <div className="max-w-3xl mx-auto space-y-6"> <div className="flex items-center gap-3"> <button onClick={() => router.push('/dashboard/inventory')} className="p-1 hover:bg-muted rounded transition-colors"> <ArrowLeft className="w-5 h-5 text-foreground" /> </button> <div> <h1 className="text-xl font-bold text-foreground">Importar Productos</h1> <p className="text-sm text-muted-foreground mt-1">Carga masiva desde archivo CSV</p> </div> </div> {/* Template download */} <div className="bg-blue-50 border border-blue-200 rounded-xl p-4 flex items-center justify-between"> <div> <p className="text-sm font-medium text-blue-800">Descarga la plantilla CSV</p> <p className="text-xs text-blue-600 mt-0.5">Columnas requeridas: name, sku</p> </div> <button onClick={downloadTemplate} className="bg-primary hover:bg-primary/90 text-white px-3 py-1.5 rounded-lg text-xs font-medium flex items-center gap-1.5 transition-all duration-150 active:scale-[0.98]"> <Download className="w-3.5 h-3.5" /> Plantilla </button> </div> {/* Upload area */} <div className="bg-card border border-border rounded-xl shadow-sm p-6 "> <input ref={fileInputRef} type="file" accept=".csv" onChange={handleFileChange} className="hidden" /> {!file ? ( <button onClick={() => fileInputRef.current?.click()} className="w-full border-2 border-dashed border-border rounded-xl p-12 text-center hover:border-primary/40 hover:bg-blue-50/50 transition-colors" > <FileSpreadsheet className="w-12 h-12 text-foreground mx-auto mb-4" /> <p className="text-sm font-medium text-foreground">Seleccionar archivo CSV</p> <p className="text-xs text-muted-foreground mt-1">Maximo 500 productos por importacion</p> </button> ) : ( <div className="space-y-4"> <div className="flex items-center justify-between bg-muted rounded-lg p-3"> <div className="flex items-center gap-3"> <FileSpreadsheet className="w-5 h-5 text-emerald-600" /> <div> <p className="text-sm font-medium text-foreground">{file.name}</p> <p className="text-xs text-muted-foreground">{preview.length} filas de vista previa</p> </div> </div> <button onClick={() => { setFile(null); setPreview([]); setResult(null); }} className="text-muted-foreground hover:text-foreground text-xs"> Cambiar archivo </button> </div> {preview.length > 0 && ( <div className="overflow-x-auto border border-border rounded-lg"> <table className="w-full text-xs"> <thead> <tr className="bg-muted border-b border-border"> <th className="text-left px-3 py-2 text-[9px] font-semibold text-muted-foreground uppercase">Nombre</th> <th className="text-left px-3 py-2 text-[9px] font-semibold text-muted-foreground uppercase">SKU</th> <th className="text-left px-3 py-2 text-[9px] font-semibold text-muted-foreground uppercase">Precio</th> </tr> </thead> <tbody> {preview.map((row, i) => ( <tr key={i} className="border-b border-border"> <td className="px-3 py-2 text-foreground">{row.name || '-'}</td> <td className="px-3 py-2 text-muted-foreground font-mono">{row.sku || '-'}</td> <td className="px-3 py-2 text-foreground">${row.sale_price || '0'}</td> </tr> ))} </tbody> </table> </div> )} <button onClick={handleImport} disabled={importing} className="w-full bg-emerald-600 hover:bg-emerald-700 disabled:opacity-50 text-white px-4 py-2.5 rounded-lg text-sm font-medium flex items-center justify-center gap-2 transition-colors" > {importing ? ( <> <div className="w-4 h-4 border-2 border-white/30 border-t-white rounded-full animate-spin" /> Importando... </> ) : ( <> <Upload className="w-4 h-4" /> Importar {preview.length > 0 ? `(${preview.length}+ productos)` : 'productos'} </> )} </button> </div> )} {error && ( <div className="mt-4 bg-rose-50 border border-rose-200 rounded-lg p-3 flex items-start gap-2"> <AlertCircle className="w-4 h-4 text-rose-600 mt-0.5 flex-shrink-0" /> <p className="text-xs text-rose-700">{error}</p> </div> )} {result && ( <div className="mt-4 space-y-3"> <div className="bg-emerald-50 border border-emerald-200 rounded-lg p-3"> <div className="flex items-center gap-2"> <CheckCircle className="w-4 h-4 text-emerald-600" /> <p className="text-sm font-medium text-emerald-800"> {result.imported} productos importados </p> </div> {result.skipped > 0 && ( <p className="text-xs text-emerald-600 mt-1"> {result.skipped} filas omitidas (SKU duplicado o datos faltantes) </p> )} </div> {result.errors && result.errors.length > 0 && ( <div className="bg-amber-50 border border-amber-200 rounded-lg p-3 max-h-40 overflow-y-auto"> <p className="text-xs font-medium text-amber-800 mb-1">Detalles:</p> {result.errors.map((err, i) => ( <p key={i} className="text-xs text-amber-700">{err}</p> ))} </div> )} <button onClick={() => router.push('/dashboard/inventory')} className="text-primary hover:text-primary text-sm font-medium" > Ver inventario → </button> </div> )} </div> </div> ); } 