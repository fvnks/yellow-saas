'use client'; import { useState, useEffect } from 'react'; import { Card, CardHeader, CardTitle, CardContent, Button, Badge, Input, Select, KPICard } from '@yellow-erp/ui'; import { ScrollText, Search, Filter, Download, Eye, Calendar, User, Activity, AlertTriangle, CheckCircle2, XCircle, Info } from 'lucide-react'; import { getApiClient } from '@/lib/api-client'; const fallbackLogs: any[] = []; const actionConfig: Record<string, { label: string; icon: typeof CheckCircle2; color: string }> = { create: { label: 'Crear', icon: CheckCircle2, color: 'text-emerald-600' }, update: { label: 'Actualizar', icon: Activity, color: 'text-blue-600' }, delete: { label: 'Eliminar', icon: XCircle, color: 'text-rose-600' }, login: { label: 'Login', icon: User, color: 'text-primary' }, config: { label: 'Config', icon: AlertTriangle, color: 'text-amber-600' }, export: { label: 'Exportar', icon: Download, color: 'text-foreground' }, }; const moduleColors: Record<string, string> = { 'Ventas': 'bg-emerald-50 text-emerald-700 border-emerald-200', 'Inventario': 'bg-blue-50 text-blue-700 border-blue-200', 'Clientes': 'bg-blue-50 text-primary border-primary/20', 'Compras': 'bg-amber-50 text-amber-700 border-amber-200', 'Facturación': 'bg-blue-50 text-blue-700 border-blue-200', 'Configuración': 'bg-muted text-foreground border-border', 'Reportes': 'bg-teal-50 text-teal-700 border-teal-200', 'Nómina': 'bg-rose-50 text-rose-700 border-rose-200', 'Almacenes': 'bg-orange-50 text-orange-700 border-orange-200', 'Proveedores': 'bg-teal-50 text-teal-700 border-teal-200', 'Auth': 'bg-blue-50 text-blue-700 border-blue-200', }; export default function AuditPage() { const [auditLogs, setAuditLogs] = useState(fallbackLogs); const [loading, setLoading] = useState(true); const [searchTerm, setSearchTerm] = useState(''); const [actionFilter, setActionFilter] = useState('all'); const [moduleFilter, setModuleFilter] = useState('all'); const [dateFilter, setDateFilter] = useState('all'); useEffect(() => { const api = getApiClient(); api.getAuditLogs() .then(res => { if (res.data && res.data.length > 0) { const mapped = res.data.map((log, index) => ({ id: index + 1, timestamp: log.created_at, user: log.user?.full_name || log.user?.email || 'unknown', action: log.action, module: log.entity_type, entity: log.entity_id || '-', details: log.details || '', ip: '-', })); setAuditLogs(mapped); } setLoading(false); }) .catch(() => setLoading(false)); }, []); const filteredLogs = auditLogs.filter(log => { const matchesSearch = log.details.toLowerCase().includes(searchTerm.toLowerCase()) || log.user.toLowerCase().includes(searchTerm.toLowerCase()) || log.entity.toLowerCase().includes(searchTerm.toLowerCase()); const matchesAction = actionFilter === 'all' || log.action === actionFilter; const matchesModule = moduleFilter === 'all' || log.module === moduleFilter; return matchesSearch && matchesAction && matchesModule; }); const modules = [...new Set(auditLogs.map(l => l.module))]; return ( <div className="space-y-6"> <div className="flex items-center justify-between"> <div> <h1 className="text-xl font-bold text-foreground">Auditoría</h1> <p className="text-sm text-muted-foreground mt-1">Registro de actividades y cambios del sistema</p> </div> <Button variant="secondary"> <Download className="w-4 h-4 mr-2" /> Exportar Log </Button> </div> <div className="grid gap-4 md:grid-cols-2 lg:grid-cols-4"> <KPICard label="Eventos Hoy" value={auditLogs.filter(l => l.timestamp.startsWith(new Date().toISOString().split('T')[0])).length} icon={Activity} trend="Registrados" trendUp={true} /> <KPICard label="Usuarios Activos" value={new Set(auditLogs.map(l => l.user)).size} icon={User} trend="Últimas 24h" trendUp={true} /> <KPICard label="Eliminaciones" value={auditLogs.filter(l => l.action === 'delete').length} icon={XCircle} trend="Última semana" trendUp={false} /> <KPICard label="Módulos Afectados" value={modules.length} icon={ScrollText} trend="Diferentes módulos" trendUp={true} /> </div> <div className="bg-card border border-border rounded-xl shadow-sm p-4 "> <div className="flex flex-wrap items-center gap-4"> <div className="relative flex-1 min-w-[200px]"> <Search className="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-muted-foreground" /> <input type="text" value={searchTerm} onChange={(e) => setSearchTerm(e.target.value)} className="w-full pl-9 pr-4 py-2 bg-muted border border-border rounded-lg text-sm focus:outline-none focus:ring-2 focus:ring-primary/20 focus:border-transparent" placeholder="Buscar en detalles, usuario o entidad..." /> </div> <Select value={actionFilter} onChange={(e) => setActionFilter(e.target.value)} options={[ { value: 'all', label: 'Todas las acciones' }, { value: 'create', label: 'Crear' }, { value: 'update', label: 'Actualizar' }, { value: 'delete', label: 'Eliminar' }, { value: 'login', label: 'Login' }, { value: 'config', label: 'Configuración' }, { value: 'export', label: 'Exportar' }, ]} /> <Select value={moduleFilter} onChange={(e) => setModuleFilter(e.target.value)} options={[ { value: 'all', label: 'Todos los módulos' }, ...modules.map(m => ({ value: m, label: m })), ]} /> </div> </div> <div className="bg-card border border-border rounded-xl shadow-sm "> <div className="overflow-x-auto"> <table className="w-full"> <thead> <tr className="border-b border-border"> <th className="text-left px-4 py-3 text-[9px] font-semibold text-muted-foreground uppercase tracking-wider">Fecha/Hora</th> <th className="text-left px-4 py-3 text-[9px] font-semibold text-muted-foreground uppercase tracking-wider">Usuario</th> <th className="text-left px-4 py-3 text-[9px] font-semibold text-muted-foreground uppercase tracking-wider">Acción</th> <th className="text-left px-4 py-3 text-[9px] font-semibold text-muted-foreground uppercase tracking-wider">Módulo</th> <th className="text-left px-4 py-3 text-[9px] font-semibold text-muted-foreground uppercase tracking-wider">Entidad</th> <th className="text-left px-4 py-3 text-[9px] font-semibold text-muted-foreground uppercase tracking-wider">Detalles</th> <th className="text-right px-4 py-3 text-[9px] font-semibold text-muted-foreground uppercase tracking-wider">IP</th> <th className="w-12 px-4 py-3"></th> </tr> </thead> <tbody> {filteredLogs.map(log => { const actionInfo = actionConfig[log.action] || actionConfig.update; const ActionIcon = actionInfo.icon; return ( <tr key={log.id} className="border-b border-border hover:bg-muted transition-colors"> <td className="px-4 py-3 text-xs font-mono text-muted-foreground whitespace-nowrap">{log.timestamp}</td> <td className="px-4 py-3 text-xs text-foreground">{log.user}</td> <td className="px-4 py-3"> <div className={`flex items-center gap-1.5 text-xs font-medium ${actionInfo.color}`}> <ActionIcon className="w-3.5 h-3.5" /> {actionInfo.label} </div> </td> <td className="px-4 py-3"> <span className={`inline-flex items-center px-2 py-0.5 rounded-full text-[9px] font-semibold border ${moduleColors[log.module] || 'bg-muted text-foreground border-border'}`}> {log.module} </span> </td> <td className="px-4 py-3 text-xs text-foreground">{log.entity}</td> <td className="px-4 py-3 text-xs text-muted-foreground max-w-xs truncate">{log.details}</td> <td className="px-4 py-3 text-xs text-muted-foreground text-right font-mono">{log.ip}</td> <td className="px-4 py-3"> <Button variant="secondary" size="sm"><Eye className="w-4 h-4" /></Button> </td> </tr> ); })} </tbody> </table> </div> </div> {filteredLogs.length === 0 && ( <div className="text-center py-12"> <ScrollText className="w-12 h-12 text-foreground mx-auto mb-3" /> <p className="text-sm text-muted-foreground">No se encontraron registros de auditoría</p> </div> )} </div> ); } 