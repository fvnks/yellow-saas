'use client'; import { useState, useEffect } from 'react'; import { useParams } from 'next/navigation'; import { CheckCircle2, Clock, FileText, AlertCircle, ExternalLink } from 'lucide-react'; export default function PortalPage() { const params = useParams(); const token = params.token as string; const [data, setData] = useState<any>(null); const [loading, setLoading] = useState(true); const [error, setError] = useState(''); useEffect(() => { fetch(`/api/portal/${token}`) .then(r => r.json()) .then(d => { if (d.error) setError(d.error); else setData(d); setLoading(false); }) .catch(() => { setError('Error al cargar'); setLoading(false); }); }, [token]); if (loading) return ( <div className="min-h-screen bg-muted flex items-center justify-center"> <div className="animate-pulse text-center"> <div className="w-16 h-16 bg-muted rounded-xl mx-auto mb-4" /> <div className="h-4 bg-muted rounded w-48 mx-auto" /> </div> </div> ); if (error) return ( <div className="min-h-screen bg-muted flex items-center justify-center"> <div className="text-center"> <AlertCircle className="w-16 h-16 text-foreground mx-auto mb-4" /> <h1 className="text-xl font-bold text-foreground">Proyecto no encontrado</h1> <p className="text-sm text-muted-foreground mt-2">{error}</p> </div> </div> ); const { project, summary, milestones, tasks, documents, change_orders, costs } = data; return ( <div className="min-h-screen bg-muted"> <div className="max-w-4xl mx-auto px-4 py-8"> <div className="bg-card border border-border rounded-xl shadow-sm p-6 mb-6"> <div className="flex items-start justify-between"> <div> <h1 className="text-2xl font-bold text-foreground">{project.name}</h1> <p className="text-sm text-muted-foreground mt-1">{project.code}</p> </div> <span className={`px-3 py-1 rounded-full text-xs font-semibold ${statusConfig[project.status]?.color || 'bg-muted text-foreground'}`}> {statusConfig[project.status]?.label || project.status} </span> </div> {project.description && <p className="text-sm text-foreground mt-4">{project.description}</p>} <div className="mt-6 grid grid-cols-2 md:grid-cols-4 gap-4"> <div className="bg-muted rounded-lg p-3"> <p className="text-[10px] font-semibold text-muted-foreground uppercase">Progreso</p> <div className="mt-2 h-2 bg-muted rounded-full overflow-hidden"> <div className="h-full bg-primary rounded-full" style={{ width: `${project.progress || 0}%` }} /> </div> <p className="text-xs text-foreground mt-1">{project.progress || 0}%</p> </div> <div className="bg-muted rounded-lg p-3"> <p className="text-[10px] font-semibold text-muted-foreground uppercase">Tareas</p> <p className="text-lg font-bold text-foreground mt-1">{summary.done_tasks}/{summary.total_tasks}</p> </div> <div className="bg-muted rounded-lg p-3"> <p className="text-[10px] font-semibold text-muted-foreground uppercase">Hitos</p> <p className="text-lg font-bold text-foreground mt-1">{summary.done_milestones}/{summary.total_milestones}</p> </div> {project.budget && ( <div className="bg-muted rounded-lg p-3"> <p className="text-[10px] font-semibold text-muted-foreground uppercase">Presupuesto</p> <p className="text-lg font-bold text-foreground mt-1">${Number(project.budget).toLocaleString('es-CL')}</p> </div> )} </div> {project.start_date && ( <div className="mt-4 flex items-center gap-4 text-xs text-muted-foreground"> <span>Inicio: {new Date(project.start_date).toLocaleDateString('es-CL')}</span> {project.end_date && <span>Fin: {new Date(project.end_date).toLocaleDateString('es-CL')}</span>} </div> )} </div> {milestones.length > 0 && ( <div className="bg-card border border-border rounded-xl shadow-sm p-6 mb-6"> <h2 className="text-sm font-semibold text-foreground mb-4">Hitos</h2> <div className="space-y-3"> {milestones.map((m: any, i: number) => ( <div key={i} className="flex items-center gap-3 p-3 bg-muted rounded-lg"> {m.status === 'completed' ? ( <CheckCircle2 className="w-4 h-4 text-emerald-500 flex-shrink-0" /> ) : ( <Clock className="w-4 h-4 text-muted-foreground flex-shrink-0" /> )} <div className="flex-1"> <p className="text-xs font-medium text-foreground">{m.name}</p> {m.description && <p className="text-[10px] text-muted-foreground">{m.description}</p>} </div> <span className="text-[10px] text-muted-foreground">{new Date(m.due_date).toLocaleDateString('es-CL')}</span> </div> ))} </div> </div> )} {documents.length > 0 && ( <div className="bg-card border border-border rounded-xl shadow-sm p-6 mb-6"> <h2 className="text-sm font-semibold text-foreground mb-4">Documentos</h2> <div className="space-y-2"> {documents.map((d: any, i: number) => ( <div key={i} className="flex items-center gap-3 p-3 bg-muted rounded-lg"> <FileText className="w-4 h-4 text-muted-foreground" /> <span className="text-xs text-foreground flex-1">{d.name}</span> {d.file_url && ( <a href={d.file_url} target="_blank" rel="noopener noreferrer" className="text-primary hover:text-primary"> <ExternalLink className="w-3.5 h-3.5" /> </a> )} </div> ))} </div> </div> )} {change_orders.length > 0 && ( <div className="bg-card border border-border rounded-xl shadow-sm p-6 mb-6"> <h2 className="text-sm font-semibold text-foreground mb-4">Ordenes de Cambio</h2> <div className="space-y-2"> {change_orders.map((co: any, i: number) => ( <div key={i} className="flex items-center gap-3 p-3 bg-muted rounded-lg"> <span className="text-xs font-mono text-muted-foreground">CO-{co.order_number}</span> <span className="text-xs text-foreground flex-1">{co.title}</span> <span className={`px-2 py-0.5 rounded-full text-[9px] font-semibold ${coStatusColors[co.status] || 'bg-muted text-foreground'}`}> {co.status} </span> </div> ))} </div> </div> )} <div className="text-center text-[10px] text-muted-foreground mt-8"> Portal de proyecto generado por Yellow ERP </dcoconst statusConfig: Record<string, { label: string; color: string }> = { planning: { label: 'Planificacion', color: 'bg-muted text-foreground' }, active: { label: 'Activo', color: 'bg-emerald-100 text-emerald-700' }, on_hold: { label: 'En Pausa', color: 'bg-amber-100 text-amber-700' }, completed: { label: 'Completado', color: 'bg-blue-100 text-blue-700' }, cancelled: { label: 'Cancelado', color: 'bg-red-100 text-red-700' }, }; 
nst coStatusColors: Record<string, string> = { pending: 'bg-amber-100 text-amber-700', approved: 'bg-emerald-100 text-emerald-700', rejected: 'bg-red-100 text-red-700', implemented: 'bg-blue-100 text-blue-700', }; 
iv> </div> </div> ); } 