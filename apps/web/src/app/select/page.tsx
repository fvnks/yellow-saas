'use client'; import { useEffect, useState, useCallback } from 'react'; import { useRouter } from 'next/navigation'; import { motion, AnimatePresence } from 'motion/react'; import Image from 'next/image'; import { Package, UsersRound, FolderKanban, Settings, CreditCard, ChevronRight, X, Lock, Zap, FlaskConical, LifeBuoy, ArrowRight, LogOut, Building2, User, ChevronDown, Mail } from 'lucide-react'; import { getApiClient } from '@/lib/api-client'; import { DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuSeparator, DropdownMenuTrigger, } from '@/components/ui/dropdown-menu'; interface ModuleOption { id: string; title: string; subtitle: string; description: string[]; icon: any; gradient: string; iconBg: string; iconColor: string; hoverBorder: string; href: string; requiredModules: string[]; moduleName: string; } interface Company { id: string; name: string; slug?: string; logo_url?: string; plan?: string; status?: string; role: string; is_default?: boolean; is_active?: boolean; } const modules: ModuleOption[] = [ { id: 'erp', title: 'ERP', subtitle: 'Gestión Empresarial', description: ['Inventario y Bodegas', 'Ventas y Compras', 'Contabilidad', 'CRM'], icon: Package, gradient: 'from-slate-700 to-slate-900', iconBg: 'bg-muted', iconColor: 'text-foreground', hoverBorder: 'hover:border-border', href: '/dashboard', requiredModules: ['inventory', 'products', 'sales', 'purchases', 'accounting', 'projects', 'crm'], moduleName: 'erp', }, { id: 'hr', title: 'RRHH', subtitle: 'Recursos Humanos', description: ['Contratos', 'Asistencia', 'Evaluaciones', 'Capacitación'], icon: UsersRound, gradient: 'from-slate-700 to-slate-900', iconBg: 'bg-muted', iconColor: 'text-foreground', hoverBorder: 'hover:border-border', href: '/hr', requiredModules: ['hr'], moduleName: 'hr', }, { id: 'projects', title: 'Proyectos', subtitle: 'Gestión de Proyectos', description: ['Cronogramas', 'Tareas', 'Tiempo', 'Reportes'], icon: FolderKanban, gradient: 'from-slate-700 to-slate-900', iconBg: 'bg-muted', iconColor: 'text-foreground', hoverBorder: 'hover:border-border', href: '/projects', requiredModules: ['projects'], moduleName: 'projects', }, { id: 'formulas', title: 'Recetas', subtitle: 'Recetas y Producción', description: ['Recetas con ingredientes', 'Producción por lotes', 'Control de stock decimal'], icon: FlaskConical, gradient: 'from-slate-700 to-slate-900', iconBg: 'bg-muted', iconColor: 'text-foreground', hoverBorder: 'hover:border-border', href: '/recetas', requiredModules: [], moduleName: 'recetas', }, { id: 'mi-cuenta', title: 'Mi Cuenta', subtitle: 'Facturación y Configuración', description: ['Mi Plan', 'Facturación', 'Módulos', 'Activaciones'], icon: CreditCard, gradient: 'from-slate-700 to-slate-900', iconBg: 'bg-muted', iconColor: 'text-foreground', hoverBorder: 'hover:border-border', href: '/mi-cuenta', requiredModules: [], moduleName: 'mi-cuenta', }, { id: 'ayuda', title: 'Ayuda', subtitle: 'Soporte y Preguntas Frecuentes', description: ['Preguntas frecuentes', 'Tickets de soporte', 'Atención a fallas'], icon: LifeBuoy, gradient: 'from-slate-700 to-slate-900', iconBg: 'bg-muted', iconColor: 'text-foreground', hoverBorder: 'hover:border-border', href: '/ayuda', requiredModules: [], moduleName: 'ayuda', }, ]; function getUserFromCookie() { if (typeof window === 'undefined') return null; const cookies = document.cookie.split(';'); const authCookie = cookies.find(c => c.trim().startsWith('auth-token=')); if (!authCookie) return null; try { const token = authCookie.split('=')[1]; const payload = JSON.parse(atob(token.split('.')[1])); return { role: payload.role || 'member', name: payload.name || 'Usuario', email: payload.email || '', }; } catch { return null; } } async function fetchUserCompanies(token: string): Promise<Company[]> { try { const res = await fetch('/api/auth/companies', { headers: { Authorization: `Bearer ${token}` }, }); const data = await res.json(); return data.data?.companies || []; } catch { return []; } } async function switchCompany(token: string, companyId: string): Promise<string | null> { try { const res = await fetch('/api/auth/switch-company', { method: 'POST', headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${token}`, }, body: JSON.stringify({ company_id: companyId }), }); const data = await res.json(); return data.data?.token || null; } catch { return null; } } function logout() { document.cookie = 'auth-token=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/;'; window.location.href = '/login'; } export default function SelectPage() { const router = useRouter(); const [company, setCompany] = useState<any>(null); const [loading, setLoading] = useState(true); const [user, setUser] = useState<any>(null); const [activatedModules, setActivatedModules] = useState<Set<string>>(new Set()); const [modalOpen, setModalOpen] = useState(false); const [selectedModule, setSelectedModule] = useState<ModuleOption | null>(null); const [activating, setActivating] = useState(false); const [lastAccess, setLastAccess] = useState<string | null>(null); const [companies, setCompanies] = useState<Company[]>([]); const [companiesLoading, setCompaniesLoading] = useState(true); const [userMenuOpen, setUserMenuOpen] = useState(false); useEffect(() => { const userData = getUserFromCookie(); if (!userData) { router.push('/login'); return; } setUser(userData); const stored = localStorage.getItem('yellow_last_access'); if (stored) { const date = new Date(stored); const now = new Date(); const diffMs = now.getTime() - date.getTime(); const diffMin = Math.floor(diffMs / 60000); if (diffMin < 1) setLastAccess('Hace menos de 1 minuto'); else if (diffMin < 60) setLastAccess(`Hace ${diffMin} minuto${diffMin > 1 ? 's' : ''}`); else { const diffH = Math.floor(diffMin / 60); if (diffH < 24) setLastAccess(`Hace ${diffH} hora${diffH > 1 ? 's' : ''}`); else { const diffD = Math.floor(diffH / 24); setLastAccess(`Hace ${diffD} día${diffD > 1 ? 's' : ''}`); } } } const token = document.cookie.split(';').find(c => c.trim().startsWith('auth-token='))?.split('=')[1]; Promise.all([ token ? fetchUserCompanies(token) : Promise.resolve([]), loadActivatedModules(), ]).then(([companiesRes, _]) => { if (companiesRes) setCompanies(companiesRes); setCompaniesLoading(false); }); const api = getApiClient(); api.getCompany().then(companyRes => { if (companyRes) setCompany(companyRes); setLoading(false); }).catch(() => setLoading(false)); }, [router]); const loadActivatedModules = async () => { try { const api = getApiClient(); const companyId = api['companyId']; const token = document.cookie.split(';').find(c => c.trim().startsWith('auth-token='))?.split('=')[1]; const res = await fetch(`/api/companies/${companyId}/modules`, { headers: { Authorization: `Bearer ${token}` }, }); const data = await res.json(); const active = new Set<string>( (data.data?.modules || []) .filter((m: any) => m.status === 'active') .map((m: any) => m.module_name) ); setActivatedModules(active); } catch (err) { console.error('Failed to load activated modules:', err); } }; const isModuleActivated = useCallback((mod: ModuleOption) => { if (mod.id === 'mi-cuenta' || mod.id === 'ayuda') return true; return activatedModules.has(mod.moduleName); }, [activatedModules]); const handleModuleClick = (mod: ModuleOption) => { if (isModuleActivated(mod)) { router.push(mod.href); } else { setSelectedModule(mod); setModalOpen(true); } }; const handleCompanySwitch = async (companyId: string) => { const token = document.cookie.split(';').find(c => c.trim().startsWith('auth-token='))?.split('=')[1]; if (!token) return; const newToken = await switchCompany(token, companyId); if (newToken) { document.cookie = `auth-token=${newToken}; path=/; max-age=604800; SameSite=Lax`; window.location.reload(); } }; const handleActivate = async () => { if (!selectedModule) return; setActivating(true); try { const api = getApiClient(); const companyId = api['companyId']; const token = document.cookie.split(';').find(c => c.trim().startsWith('auth-token='))?.split('=')[1]; for (const moduleName of selectedModule.requiredModules) { await fetch(`/api/companies/${companyId}/modules/activate`, { method: 'POST', headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${token}` }, body: JSON.stringify({ module_name: moduleName }), }); } await loadActivatedModules(); setModalOpen(false); router.push(selectedModule.href); } catch (err) { console.error('Failed to activate module:', err); } setActivating(false); }; if (loading) { return ( <div className="min-h-screen bg-muted flex items-center justify-center"> <div className="flex flex-col items-center gap-3"> <div className="w-8 h-8 border-2 border-border border-t-slate-900 rounded-full animate-spin" /> <p className="text-xs text-muted-foreground">Cargando...</p> </div> </div> ); } return ( <div className="min-h-screen bg-muted flex flex-col"> {/* Header */} <div className="bg-card border-b border-border px-6 py-4"> <div className="max-w-5xl mx-auto flex items-center justify-between"> <div className="flex items-center gap-3"> <div className="w-9 h-9 rounded-lg flex items-center justify-center overflow-hidden bg-primary"> <Image src="/logo/yellow-cube.svg" alt="Yellow" width={28} height={28} className="brightness-0 invert" /> </div> <div> <h1 className="text-sm font-semibold text-foreground">{company?.name || 'Yellow ERP'}</h1> <p className="text-[11px] text-muted-foreground">Bienvenido, {user?.name}</p> </div> </div> <DropdownMenu> <DropdownMenuTrigger asChild> <button className="flex items-center gap-2 px-3 py-1.5 rounded-lg hover:bg-muted transition-colors"> <div className="w-7 h-7 rounded-full bg-primary/10 flex items-center justify-center flex-shrink-0"> <User className="w-4 h-4 text-primary" /> </div> <span className="text-xs font-medium text-foreground hidden sm:block">{user?.name}</span> <ChevronDown className="w-3.5 h-3.5 text-muted-foreground" /> </button> </DropdownMenuTrigger> <DropdownMenuContent side="bottom" align="end" className="w-56"> {/* User Info */} <div className="px-3 py-2 border-b border-border"> <div className="flex items-center gap-2"> <div className="w-8 h-8 rounded-full bg-primary/10 flex items-center justify-center flex-shrink-0"> <User className="w-4 h-4 text-primary" /> </div> <div className="flex-1 min-w-0"> <p className="text-sm font-semibold text-foreground truncate">{user?.name}</p> <p className="text-[10px] text-muted-foreground truncate flex items-center gap-1"> <Mail className="w-3 h-3 flex-shrink-0" /> {user?.email} </p> </div> </div> </div> {/* Company Switcher */} {!companiesLoading && companies.length > 1 && ( <> <DropdownMenuSeparator /> <div className="px-3 py-2"> <p className="text-[10px] font-semibold text-muted-foreground uppercase tracking-wider"> Cambiar Empresa </p> </div> <DropdownMenuSeparator /> {companies.map((c) => ( <DropdownMenuItem key={c.id} onClick={() => handleCompanySwitch(c.id)} className="cursor-pointer flex items-center gap-2 px-2 py-2" disabled={c.id === company?.id} > <div className="w-7 h-7 bg-gradient-to-br from-yellow-400 to-amber-500 rounded-lg flex items-center justify-center flex-shrink-0"> <Building2 className="w-3.5 h-3.5 text-white" /> </div> <div className="flex-1 min-w-0"> <p className="text-sm font-medium text-foreground truncate">{c.name}</p> <p className="text-[10px] text-muted-foreground capitalize">{c.role}</p> </div> {c.id === company?.id && ( <svg className="w-4 h-4 text-emerald-500 flex-shrink-0" fill="none" stroke="currentColor" viewBox="0 0 24 24"> <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M5 13l4 4L19 7" /> </svg> )} </DropdownMenuItem> ))} </> )} <DropdownMenuSeparator /> <DropdownMenuItem onClick={logout} className="flex items-center gap-2 px-2 py-2 text-rose-600 hover:bg-rose-50" > <LogOut className="w-4 h-4" /> <span>Cerrar sesión</span> </DropdownMenuItem> </DropdownMenuContent> </DropdownMenu> </div> </div> {/* Content */} <div className="flex-1 flex items-center justify-center p-6"> <div className="max-w-4xl w-full"> <motion.div initial={{ opacity: 0, y: 12 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.3 }} className="text-center mb-10" > <h2 className="text-2xl font-bold text-foreground tracking-tight"> Hola {user?.name || 'Usuario'} </h2> <p className="text-sm text-muted-foreground mt-2"> Selecciona un módulo para comenzar </p> </motion.div> <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4"> {modules.filter(mod => isModuleActivated(mod)).length === 0 ? ( <div className="col-span-full text-center py-16"> <div className="w-16 h-16 bg-muted rounded-xl flex items-center justify-center mx-auto mb-4"> <Lock className="w-7 h-7 text-muted-foreground" /> </div> <p className="text-sm font-medium text-foreground">No hay módulos activos</p> <p className="text-xs text-muted-foreground mt-1">Contacta al administrador para activar módulos</p> </div> ) : modules.filter(mod => isModuleActivated(mod)).map((mod, i) => { const Icon = mod.icon; const activated = isModuleActivated(mod); return ( <motion.button key={mod.id} initial={{ opacity: 0, y: 12 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.25, delay: 0.05 + i * 0.05 }} onClick={() => handleModuleClick(mod)} className="group relative bg-card border border-border hover:border-border rounded-xl p-5 text-left transition-all duration-200 hover:shadow-lg hover:shadow-slate-200/50" > <div className="flex items-start gap-4"> <div className={`w-11 h-11 ${mod.iconBg} rounded-xl flex items-center justify-center flex-shrink-0 transition-transform duration-200 group-hover:scale-105`}> <Icon className={`w-5 h-5 ${mod.iconColor}`} /> </div> <div className="flex-1 min-w-0"> <div className="flex items-center justify-between"> <div> <h3 className="text-sm font-semibold text-foreground">{mod.title}</h3> <p className="text-[11px] text-muted-foreground mt-0.5">{mod.subtitle}</p> </div> {activated ? ( <ArrowRight className="w-4 h-4 text-foreground group-hover:text-muted-foreground group-hover:translate-x-0.5 transition-all duration-150 flex-shrink-0" /> ) : ( <Lock className="w-4 h-4 text-muted-foreground flex-shrink-0" /> )} </div> <div className="flex flex-wrap gap-1.5 mt-3"> {mod.description.map((item, j) => ( <span key={j} className="inline-flex items-center px-2 py-0.5 bg-muted text-foreground text-[10px] font-medium rounded-md border border-border"> {item} </span> ))} </div> </div> </div> </motion.button> ); })} </div> <motion.p initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ duration: 0.3, delay: 0.3 }} className="text-center text-[11px] text-muted-foreground mt-8" > Puedes cambiar de módulo en cualquier momento desde el menú lateral </motion.p> </div> </div> {/* Activation Modal */} <AnimatePresence> {modalOpen && selectedModule && ( <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} className="fixed inset-0 bg-black/40 backdrop-blur-sm flex items-center justify-center z-50 p-4" onClick={() => setModalOpen(false)} > <motion.div initial={{ opacity: 0, scale: 0.95, y: 12 }} animate={{ opacity: 1, scale: 1, y: 0 }} exit={{ opacity: 0, scale: 0.95, y: 12 }} transition={{ duration: 0.15 }} onClick={(e) => e.stopPropagation()} className="bg-card rounded-xl shadow-xl w-full max-w-sm" > <div className="p-6 text-center"> <div className="w-14 h-14 bg-muted rounded-xl flex items-center justify-center mx-auto mb-4"> <Lock className="w-6 h-6 text-muted-foreground" /> </div> <h2 className="text-base font-semibold text-foreground">Módulo no activado</h2> <p className="text-sm text-muted-foreground mt-2"> <span className="font-medium text-foreground">{user?.name}</span>, el módulo{' '} <span className="font-medium text-foreground">{selectedModule.title}</span> aún no está activado. </p> <p className="text-sm text-muted-foreground mt-1"> ¿Deseas activarlo ahora? </p> </div> <div className="px-6 pb-6 flex gap-3"> <button onClick={() => setModalOpen(false)} className="flex-1 bg-card border border-border hover:bg-muted text-foreground px-4 py-2.5 rounded-lg text-sm font-medium transition-colors" > Ahora no </button> <button onClick={handleActivate} disabled={activating} className="flex-1 bg-primary hover:bg-primary/90 text-white px-4 py-2.5 rounded-lg text-sm font-medium flex items-center justify-center gap-2 transition-colors disabled:opacity-50" > {activating ? ( <div className="w-4 h-4 border-2 border-white/30 border-t-white rounded-full animate-spin" /> ) : ( <> <Zap className="w-4 h-4" /> Activar </> )} </button> </div> </motion.div> </motion.div> )} </AnimatePresence> {/* Last Access */} {lastAccess && ( <div className="fixed bottom-4 right-5 text-[10px] text-muted-foreground select-none"> Último acceso: {lastAccess} </div> )} </div> ); } 