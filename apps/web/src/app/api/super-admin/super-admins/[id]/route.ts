import { query } from '@/api/lib/db';
import { successResponse, errorResponse } from '@/api/lib/helpers';
import { NextRequest } from 'next/server';
import { verifySuperAdmin } from '@/api/super-admin/lib/auth'; export async function PUT(request: NextRequest, { params }: { params: { id: string } }) { const admin = await verifySuperAdmin(request); if (!admin) return errorResponse('No autorizado', 401); const { id } = params; const body = await request.json(); const { name, email, is_active, password } = body; try { const existing = await query('SELECT id FROM super_admins WHERE id = $1', [id]); if (existing.rows.length === 0) return errorResponse('Super admin no encontrado', 404); if (email) { const emailCheck = await query('SELECT id FROM super_admins WHERE email = $1 AND id != $2', [email, id]); if (emailCheck.rows.length > 0) return errorResponse('Ya existe otro admin con ese email', 409); } const updates: string[] = []; const values: any[] = []; let idx = 1; if (name !== undefined) { updates.push(`name = $${idx++}`); values.push(name); } if (email !== undefined) { updates.push(`email = $${idx++}`); values.push(email); } if (is_active !== undefined) { updates.push(`is_active = $${idx++}`); values.push(is_active); } if (password) { if (password.length < 8) return errorResponse('La contraseña debe tener al menos 8 caracteres', 400); const bcrypt = await import('bcryptjs'); const passwordHash = await bcrypt.hash(password, 12); updates.push(`password_hash = $${idx++}`); values.push(passwordHash); } if (updates.length === 0) return errorResponse('Sin cambios para actualizar', 400); updates.push(`updated_at = now()`); values.push(id); await query(`UPDATE super_admins SET ${updates.join(', ')} WHERE id = $${idx}`, values); return successResponse({ success: true }); } catch (err) { console.error('Super admin update error:', err); return errorResponse('Error al actualizar super admin', 500); }
} export async function DELETE(request: NextRequest, { params }: { params: { id: string } }) { const admin = await verifySuperAdmin(request); if (!admin) return errorResponse('No autorizado', 401); const { id } = params; if (id === admin.id) return errorResponse('No puedes eliminarte a ti mismo', 400); try { const existing = await query('SELECT id FROM super_admins WHERE id = $1', [id]); if (existing.rows.length === 0) return errorResponse('Super admin no encontrado', 404); await query('DELETE FROM super_admins WHERE id = $1', [id]); return successResponse({ success: true }); } catch (err) { console.error('Super admin delete error:', err); return errorResponse('Error al eliminar super admin', 500); }
}
