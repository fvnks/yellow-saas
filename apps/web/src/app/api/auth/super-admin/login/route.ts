import { query } from '@/api/lib/db'; import { successResponse, errorResponse } from '@/api/lib/helpers'; import { NextRequest } from 'next/server'; import bcrypt from 'bcryptjs'; import { SignJWT } from 'jose'; import { getJwtSecret } from '@/lib/env'; const JWT_SECRET = getJwtSecret(); export async function POST(request: NextRequest) { try { const body = await request.json(); const { email, password } = body; if (!email || !password) { return errorResponse('Email y contraseña son requeridos', 400); } const result = await query( 'SELECT id, email, name, password_hash, is_active FROM super_admins WHERE email = $1', [email] ); if (result.rows.length === 0) { return errorResponse('Credenciales inválidas', 401); } const admin = result.rows[0]; if (!admin.is_active) { return errorResponse('Cuenta desactivada', 403); } if (!admin.password_hash) { return errorResponse('Cuenta sin contraseña configurada', 401); } const validPassword = await bcrypt.compare(password, admin.password_hash); if (!validPassword) { return errorResponse('Credenciales inválidas', 401); } await query( 'UPDATE super_admins SET last_login_at = now() WHERE id = $1', [admin.id] ); const token = await new SignJWT({ id: admin.id, email: admin.email, name: admin.name, role_type: 'super_admin', role: 'super_admin', }) .setProtectedHeader({ alg: 'HS256' }) .setIssuedAt() .setExpirationTime('7d') .sign(JWT_SECRET); return successResponse({ token, user: { id: admin.id, email: admin.email, name: admin.name, role_type: 'super_admin', }, }); } catch (err) { console.error('Super admin login error:', err); return errorResponse(err instanceof Error ? err.message : 'Internal server error', 500); } } 