import { NextResponse } from 'next/server'; import { query } from '@/api/lib/db'; const MIGRATION_SECRET = process.env.JWT_SECRET; if (!MIGRATION_SECRET) { throw new Error('La variable de entorno JWT_SECRET es requerida. Configúrala antes de iniciar la aplicación.'); } export async function POST(request: Request) { try { const body = await request.json(); if (body.secret !== MIGRATION_SECRET) { return NextResponse.json({ error: 'Unauthorized' }, { status: 401 }); } const results: string[] = []; const fixes = [ `ALTER TABLE notifications ADD COLUMN IF NOT EXISTS read_at TIMESTAMPTZ`, `DO $$ BEGIN IF NOT EXISTS (SELECT 1 FROM pg_indexes WHERE indexname = 'idx_notifications_unread') THEN CREATE INDEX idx_notifications_unread ON notifications(user_id, read_at) WHERE read_at IS NULL; END IF; END $$`, `CREATE TABLE IF NOT EXISTS purchase_registers ( id UUID PRIMARY KEY DEFAULT gen_random_uuid(), company_id UUID NOT NULL REFERENCES companies(id) ON DELETE CASCADE, razon_social TEXT NOT NULL, rut TEXT, invoice_number TEXT NOT NULL, emission_date DATE NOT NULL DEFAULT CURRENT_DATE, status TEXT DEFAULT 'no_pagada' CHECK (status IN ('pagada', 'no_pagada')), amount DECIMAL(14,2) DEFAULT 0, area TEXT NOT NULL CHECK (area IN ('LOGISTICA', 'VERTIKAL', 'CASA', 'BRONCES')), payment_type TEXT NOT NULL CHECK (payment_type IN ('tarjeta_credito', 'tarjeta_debito', 'transferencia', 'efectivo', 'cheque', 'otro')), payment_date DATE, notes TEXT, created_at TIMESTAMPTZ DEFAULT now(), updated_at TIMESTAMPTZ DEFAULT now(), UNIQUE (company_id, invoice_number) )`, `ALTER TABLE purchase_order_items DROP COLUMN IF EXISTS variant_id`, ]; for (const sql of fixes) { try { await query(sql); results.push(`OK: ${sql.substring(0, 80)}...`); } catch (err: any) { results.push(`WARN: ${sql.substring(0, 60)}... → ${err.message}`); } } return NextResponse.json({ success: true, results }); } catch (err: any) { return NextResponse.json({ error: err.message }, { status: 500 }); } } 