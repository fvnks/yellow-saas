import { query } from '@/api/lib/db';
import { getCompanyId, successResponse, errorResponse } from '@/api/lib/helpers';
import { NextRequest } from 'next/server'; export async function GET(req: NextRequest, { params }: { params: { id: string } }) { try { const companyId = await getCompanyId(req); if (!companyId) return errorResponse('Company ID not found', 400); const url = new URL(req.url); const customerId = url.searchParams.get('customerId'); const productId = url.searchParams.get('productId'); if (!customerId) return errorResponse('customerId is required', 400); let whereClause = `WHERE so.company_id = $1 AND so.customer_id = $2 AND so.status != 'cancelled'`; const params: any[] = [companyId, customerId]; if (productId) { whereClause += ` AND soi.product_id = $3`; params.push(productId); } const { rows: history } = await query( `SELECT soi.product_id, p.name as product_name, p.sku, soi.unit_price, soi.quantity, soi.line_total, so.created_at as order_date, so.order_number, c.name as customer_name FROM sales_order_items soi JOIN sales_orders so ON so.id = soi.order_id JOIN products p ON p.id = soi.product_id JOIN customers c ON c.id = so.customer_id ${whereClause} ORDER BY so.created_at DESC LIMIT 200`, params ); const productPrices = history.reduce((acc, h) => { const key = h.product_id; if (!acc[key]) acc[key] = { name: h.product_name, sku: h.sku, prices: [], minPrice: Infinity, maxPrice: 0, avgPrice: 0, totalQty: 0 }; acc[key].prices.push({ price: parseFloat(h.unit_price), date: h.order_date, order: h.order_number, qty: parseInt(h.quantity) }); acc[key].minPrice = Math.min(acc[key].minPrice, parseFloat(h.unit_price)); acc[key].maxPrice = Math.max(acc[key].maxPrice, parseFloat(h.unit_price)); acc[key].totalQty += parseInt(h.quantity); return acc; }, {} as Record<string, any>); for (const key of Object.keys(productPrices)) { const pp = productPrices[key]; pp.avgPrice = pp.prices.reduce((sum: number, p: any) => sum + p.price * p.qty, 0) / pp.totalQty; } return successResponse({ history, productPrices: Object.values(productPrices) }); } catch (e: any) { return errorResponse(e.message, 500); }
}
