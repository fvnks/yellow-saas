import { query } from '@/api/lib/db';
import { getCompanyId, successResponse, errorResponse } from '@/api/lib/helpers';
import { NextRequest } from 'next/server'; export async function GET( request: NextRequest, { params }: { params: { id: string; returnId: string } }
) { try { const companyId = await getCompanyId(request); if (!companyId) return errorResponse('Company ID not found', 400); const result = await query( `SELECT cr.*, json_build_object('id', c.id, 'name', c.name, 'tax_id', c.tax_id) as customer, json_build_object('id', w.id, 'name', w.name, 'code', w.code) as warehouse, (SELECT json_agg(json_build_object( 'id', cri.id, 'product_id', cri.product_id, 'quantity', cri.quantity, 'unit_price', cri.unit_price, 'restock', cri.restock, 'product', (SELECT json_build_object('id', p.id, 'name', p.name, 'sku', p.sku) FROM products p WHERE p.id = cri.product_id) )) FROM customer_return_items cri WHERE cri.return_id = cr.id) as items FROM customer_returns cr LEFT JOIN customers c ON cr.customer_id = c.id LEFT JOIN warehouses w ON cr.warehouse_id = w.id WHERE cr.id = $1 AND cr.company_id = $2`, [params.returnId, companyId] ); if (result.rows.length === 0) return errorResponse('Return not found', 404); return successResponseconst validStatuses = ['pending', 'approved', 'completed', 'rejected', 'cancelled']; 
(result.rows[0]); } catch { return errorResponse('Internal server error', 500); }
} export async function PUT( request: NextRequest, { params }: { params: { id: string; returnId: string } }
) { try { const companyId = await getCompanyId(request); if (!companyId) return errorResponse('Company ID not found', 400); const body = await request.json(); const { status, reason, notes } = body; if (status) { if (!validStatuses.includes(status)) { return errorResponse(`Invalid status. Must be one of: ${validStatuses.join(', ')}`, 400); } } const result = await query( `UPDATE customer_returns SET status = COALESCE($1, status), reason = COALESCE($2, reason), notes = COALESCE($3, notes) WHERE id = $4 AND company_id = $5 RETURNING *`, [status || null, reason || null, notes || null, params.returnId, companyId] ); if (result.rows.length === 0) return errorResponse('Return not found', 404); return successResponse(result.rows[0]); } catch { return errorResponse('Internal server error', 500); }
}
