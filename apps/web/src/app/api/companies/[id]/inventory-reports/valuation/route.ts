import { query } from '@/api/lib/db';
import { getCompanyId, successResponse, errorResponse } from '@/api/lib/helpers';
import { NextRequest } from 'next/server'; export async function GET(req: NextRequest, { params }: { params: { id: string } }) { try { const companyId = await getCompanyId(req); if (!companyId) return errorResponse('Company ID not found', 400); const { searchParams } = new URL(req.url); const method = searchParams.get('method') || 'weighted_avg'; const warehouseId = searchParams.get('warehouse_id'); let sql = ` SELECT p.id, p.name, p.sku, p.cost_price, COALESCE(SUM(sl.quantity), 0) as current_stock, CASE WHEN COALESCE(SUM(sl.quantity), 0) > 0 THEN CASE WHEN $2 = 'weighted_avg' THEN COALESCE( (SELECT SUM(sm.quantity * sm.unit_cost) / NULLIF(SUM(sm.quantity), 0) FROM stock_movements sm WHERE sm.product_id = p.id AND sm.company_id = p.company_id AND sm.type = 'in' AND sm.unit_cost > 0), p.cost_price ) ELSE p.cost_price END ELSE 0 END as avg_cost, COALESCE(SUM(sl.quantity), 0) * CASE WHEN $2 = 'weighted_avg' THEN COALESCE( (SELECT SUM(sm.quantity * sm.unit_cost) / NULLIF(SUM(sm.quantity), 0) FROM stock_movements sm WHERE sm.product_id = p.id AND sm.company_id = p.company_id AND sm.type = 'in' AND sm.unit_cost > 0), p.cost_price ) ELSE p.cost_price END as total_value FROM products p LEFT JOIN stock_levels sl ON sl.product_id = p.id AND sl.company_id = p.company_id WHERE p.company_id = $1 AND p.is_active = TRUE `; const sqlParams: any[] = [companyId, method]; let idx = 3; if (warehouseId) { sql += ` AND sl.warehouse_id = $${idx}`; sqlParams.push(warehouseId); idx++; } sql += ` GROUP BY p.id, p.name, p.sku, p.cost_price HAVING COALESCE(SUM(sl.quantity), 0) > 0 ORDER BY total_value DESC `; const { rows } = await query(sql, sqlParams); const totalValue = rows.reduce((sum: number, r: any) => sum + parseFloat(r.total_value || 0), 0); const totalStock = rows.reduce((sum: number, r: any) => sum + parseFloat(r.current_stock || 0), 0); return successResponse({ products: rows, totalValue, totalStock, method, count: rows.length, }); } catch (e: any) { return errorResponse(e.message, 500); }
}
