import { query } from '@/api/lib/db';
import { getCompanyId, successResponse, errorResponse } from '@/api/lib/helpers';
import { NextRequest } from 'next/server'; export async function GET( request: NextRequest, { params }: { params: { id: string; allocationId: string } }
) { try { const companyId = await getCompanyId(request); if (!companyId) return errorResponse('Company ID not found', 400); const result = await query( `SELECT lca.*, json_build_object('id', po.id, 'order_number', po.order_number) as purchase_order, (SELECT json_agg(json_build_object( 'id', lci.id, 'product_id', lci.product_id, 'product_name', p.name, 'product_sku', p.sku, 'allocated_amount', lci.allocated_amount, 'base_value', lci.base_value, 'base_weight', lci.base_weight, 'base_volume', lci.base_volume, 'base_quantity', lci.base_quantity )) FROM landed_cost_items lci JOIN products p ON lci.product_id = p.id WHERE lci.allocation_id = lca.id) as items FROM landed_cost_allocations lca LEFT JOIN purchase_orders po ON lca.purchase_order_id = po.id WHERE lca.id = $1 AND lca.company_id = $2`, [params.allocationId, companyId] ); if (result.rows.length === 0) return errorResponse('Landed cost allocation not found', 404); return successResponse(result.rows[0]); } catch (err) { console.error('Get landed cost allocation error:', err); return errorResponse('Internal server error', 500); }
} export async function PUT( request: NextRequest, { params }: { params: { id: string; allocationId: string } }
) { try { const companyId = await getCompanyId(request); if (!companyId) return errorResponse('Company ID not found', 400); const body = await request.json(); const { cost_type, amount, currency, exchange_rate, allocation_method, description, items } = body; const validCostTypes = ['freight', 'insurance', 'customs_duty', 'handling', 'other']; if (cost_type && !validCostTypes.includes(cost_type)) { return errorResponse(`cost_type must be one of: ${validCostTypes.join(', ')}`, 400); } const validMethods = ['value', 'weight', 'volume', 'quantity']; if (allocation_method && !validMethods.includes(allocation_method)) { return errorResponse(`allocation_method must be one of: ${validMethods.join(', ')}`, 400); } const result = await query( `UPDATE landed_cost_allocations SET cost_type = COALESCE($1, cost_type), amount = COALESCE($2, amount), currency = COALESCE($3, currency), exchange_rate = COALESCE($4, exchange_rate), allocation_method = COALESCE($5, allocation_method), description = COALESCE($6, description), updated_at = NOW() WHERE id = $7 AND company_id = $8 RETURNING *`, [cost_type, amount, currency, exchange_rate, allocation_method, description, params.allocationId, companyId] ); if (result.rows.length === 0) return errorResponse('Allocation not found', 404); if (items && Array.isArray(items)) { await query('DELETE FROM landed_cost_items WHERE allocation_id = $1', [params.allocationId]); for (const item of items) { await query( `INSERT INTO landed_cost_items (company_id, allocation_id, product_id, allocated_amount, base_value, base_weight, base_volume, base_quantity) VALUES ($1, $2, $3, $4, $5, $6, $7, $8)`, [companyId, params.allocationId, item.product_id, item.allocated_amount, item.base_value || null, item.base_weight || null, item.base_volume || null, item.base_quantity || null] ); } } return successResponse(result.rows[0]); } catch (err) { console.error('Update landed cost allocation error:', err); return errorResponse('Internal server error', 500); }
} export async function DELETE( request: NextRequest, { params }: { params: { id: string; allocationId: string } }
) { try { const companyId = await getCompanyId(request); if (!companyId) return errorResponse('Company ID not found', 400); const result = await query( 'DELETE FROM landed_cost_allocations WHERE id = $1 AND company_id = $2 RETURNING id', [params.allocationId, companyId] ); if (result.rows.length === 0) return errorResponse('Allocation not found', 404); return successResponse({ message: 'Allocation deleted successfully' }); } catch (err) { console.error('Delete landed cost allocation error:', err); return errorResponse('Internal server error', 500); }
}