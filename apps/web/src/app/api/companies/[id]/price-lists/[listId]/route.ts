import { query } from '@/api/lib/db';
import { getCompanyId, successResponse, errorResponse } from '@/api/lib/helpers';
import { NextRequest } from 'next/server'; export async function GET(req: NextRequest, { params }: { params: { id: string; listId: string } }) { try { const companyId = await getCompanyId(req); if (!companyId) return errorResponse('Company ID not found', 400); const { rows: list } = await query( 'SELECT * FROM price_lists WHERE id = $1 AND company_id = $2', [params.listId, companyId] ); if (list.length === 0) return errorResponse('Price list not found', 404); const { rows: items } = await query( `SELECT pli.*, p.name as product_name, p.sku, p.sale_price FROM price_list_items pli JOIN products p ON p.id = pli.product_id WHERE pli.price_list_id = $1 ORDER BY p.name`, [params.listId] ); return successResponse({ ...list[0], items }); } catch (e: any) { return errorResponse(e.message, 500); }
} export async function PATCH(req: NextRequest, { params }: { params: { id: string; listId: string } }) { try { const companyId = await getCompanyId(req); if (!companyId) return errorResponse('Company ID not found', 400); const body = await req.json(); const { name, description, is_default, is_active, valid_from, valid_until, items } = body; if (is_default) { await query('UPDATE price_lists SET is_default = FALSE WHERE company_id = $1 AND id != $2', [companyId, params.listId]); } const fields: string[] = []; const values: any[] = []; let idx = 3; if (name !== undefined) { fields.push(`name = $${idx}`); values.push(name); idx++; } if (description !== undefined) { fields.push(`description = $${idx}`); values.push(description); idx++; } if (is_default !== undefined) { fields.push(`is_default = $${idx}`); values.push(is_default); idx++; } if (is_active !== undefined) { fields.push(`is_active = $${idx}`); values.push(is_active); idx++; } if (valid_from !== undefined) { fields.push(`valid_from = $${idx}`); values.push(valid_from); idx++; } if (valid_until !== undefined) { fields.push(`valid_until = $${idx}`); values.push(valid_until); idx++; } if (fields.length > 0) { await query( `UPDATE price_lists SET ${fields.join(', ')} WHERE id = $1 AND company_id = $2`, [params.listId, companyId, ...values] ); } if (Array.isArray(items)) { await query('DELETE FROM price_list_items WHERE price_list_id = $1 AND company_id = $2', [params.listId, companyId]); for (const item of items) { if (item.product_id && item.unit_price) { await query( `INSERT INTO price_list_items (company_id, price_list_id, product_id, unit_price, min_quantity, discount_pct) VALUES ($1, $2, $3, $4, $5, $6)`, [companyId, params.listId, item.product_id, item.unit_price, item.min_quantity || 1, item.discount_pct || 0] ); } } } const { rows } = await query('SELECT * FROM price_lists WHERE id = $1 AND company_id = $2', [params.listId, companyId]); return successResponse(rows[0]); } catch (e: any) { return errorResponse(e.message, 500); }
} export async function DELETE(req: NextRequest, { params }: { params: { id: string; listId: string } }) { try { const companyId = await getCompanyId(req); if (!companyId) return errorResponse('Company ID not found', 400); await query('DELETE FROM price_list_items WHERE price_list_id = $1 AND company_id = $2', [params.listId, companyId]); await query('DELETE FROM price_lists WHERE id = $1 AND company_id = $2', [params.listId, companyId]); return successResponse({ deleted: true }); } catch (e: any) { return errorResponse(e.message, 500); }
}
