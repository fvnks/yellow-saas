import { query } from '@/api/lib/db';
import { getCompanyId, successResponse, errorResponse } from '@/api/lib/helpers';
import { NextRequest } from 'next/server'; export async function GET(req: NextRequest, { params }: { params: { id: string } }) { try { const companyId = await getCompanyId(req); if (!companyId) return errorResponse('Company ID not found', 400); const { rows } = await query( `SELECT pl.*, (SELECT COUNT(*) FROM price_list_items pli WHERE pli.price_list_id = pl.id) as item_count FROM price_lists pl WHERE pl.company_id = $1 ORDER BY pl.is_default DESC, pl.name`, [companyId] ); return successResponse(rows); } catch (e: any) { return errorResponse(e.message, 500); }
} export async function POST(req: NextRequest, { params }: { params: { id: string } }) { try { const companyId = await getCompanyId(req); if (!companyId) return errorResponse('Company ID not found', 400); const body = await req.json(); const { name, description, currency = 'CLP', is_default = false, valid_from, valid_until, items = [] } = body; if (!name) return errorResponse('name required', 400); if (is_default) { await query('UPDATE price_lists SET is_default = FALSE WHERE company_id = $1', [companyId]); } const { rows } = await query( `INSERT INTO price_lists (company_id, name, description, currency, is_default, valid_from, valid_until) VALUES ($1, $2, $3, $4, $5, $6, $7) RETURNING *`, [companyId, name, description || '', currency, is_default, valid_from || null, valid_until || null] ); for (const item of items) { if (item.product_id && item.unit_price) { await query( `INSERT INTO price_list_items (company_id, price_list_id, product_id, unit_price, min_quantity, discount_pct) VALUES ($1, $2, $3, $4, $5, $6)`, [companyId, rows[0].id, item.product_id, item.unit_price, item.min_quantity || 1, item.discount_pct || 0] ); } } return successResponse(rows[0], 201); } catch (e: any) { return errorResponse(e.message, 500); }
}
