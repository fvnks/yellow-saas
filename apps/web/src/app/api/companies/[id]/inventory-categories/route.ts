import { query } from '@/api/lib/db';
import { getCompanyId, successResponse, errorResponse, parseSearchParams, paginatedResponse } from '@/api/lib/helpers';
import { NextRequest } from 'next/server'; export async function GET(request: NextRequest) { try { const companyId = await getCompanyId(request); if (!companyId) return errorResponse('Company ID not found', 400); const { page, limit, search, offset } = parseSearchParams(request); let whereClause = 'WHERE ic.company_id = $1'; const params: any[] = [companyId]; let paramIndex = 2; if (search) { whereClause += ` AND ic.name ILIKE $${paramIndex}`; params.push(`%${search}%`); paramIndex++; } const countResult = await query( `SELECT COUNT(*) FROM inventory_categories ic ${whereClause}`, params ); const dataResult = await query( `SELECT ic.*, (SELECT COUNT(*) FROM products WHERE category_id = ic.id) as product_count FROM inventory_categories ic ${whereClause} ORDER BY ic.sort_order ASC, ic.name ASC LIMIT $${paramIndex} OFFSET $${paramIndex + 1}`, [...params, limit, offset] ); return paginatedResponse(dataResult.rows, parseInt(countResult.rows[0].count), page, limit); } catch { return errorResponse('Internal server error', 500); }
} export async function POST(request: NextRequest) { try { const companyId = await getCompanyId(request); if (!companyId) return errorResponse('Company ID not found', 400); const body = await request.json(); const { name, description, color, icon, sort_order } = body; if (!name) return errorResponse('Name is required', 400); const existing = await query( `SELECT id FROM inventory_categories WHERE company_id = $1 AND LOWER(name) = LOWER($2)`, [companyId, name] ); if (existing.rows.length > 0) { return errorResponse('A category with this name already exists', 400); } const result = await query( `INSERT INTO inventory_categories (company_id, name, description, color, icon, sort_order) VALUES ($1, $2, $3, $4, $5, $6) RETURNING *`, [companyId, name, description || null, color || '#6366f1', icon || 'tag', sort_order || 0] ); return successResponse(result.rows[0], 201); } catch { return errorResponse('Internal server error', 500); }
}
