import { query } from '@/api/lib/db';
import { getCompanyId, successResponse, errorResponse } from '@/api/lib/helpers';
import { NextRequest } from 'next/server'; export async function GET(req: NextRequest, { params }: { params: { id: string } }) { try { const companyId = await getCompanyId(req); if (!companyId) return errorResponse('Company ID not found', 400); const { searchParams } = new URL(req.url); const status = searchParams.get('status'); let sql = ` SELECT st.*, sw.name as from_warehouse_name, sw.code as from_warehouse_code, dw.name as to_warehouse_name, dw.code as to_warehouse_code FROM stock_transfers st LEFT JOIN warehouses sw ON sw.id = st.source_warehouse_id LEFT JOIN warehouses dw ON dw.id = st.destination_warehouse_id WHERE st.company_id = $1 `; const sqlParams: any[] = [companyId]; if (status) { sql += ' AND st.status = $2'; sqlParams.push(status); } sql += ' ORDER BY st.created_at DESC'; const { rows } = await query(sql, sqlParams); return successResponse(rows); } catch (e: any) { return errorResponse(e.message, 500); }
} export async function POST(req: NextRequest, { params }: { params: { id: string } }) { try { const companyId = await getCompanyId(req); if (!companyId) return errorResponse('Company ID not found', 400); const body = await req.json(); const { product_id, from_warehouse_id, to_warehouse_id, quantity, notes } = body; if (!from_warehouse_id || !to_warehouse_id) { return errorResponse('from_warehouse_id, to_warehouse_id required', 400); } if (from_warehouse_id === to_warehouse_id) { return errorResponse('Cannot transfer to same warehouse', 400); } const transferNumber = `TRF-${Date.now()}`; const { rows } = await query( `INSERT INTO stock_transfers (company_id, transfer_number, source_warehouse_id, destination_warehouse_id, notes) VALUES ($1, $2, $3, $4, $5) RETURNING *`, [companyId, transferNumber, from_warehouse_id, to_warehouse_id, notes || ''] ); if (product_id && quantity) { await query( `INSERT INTO stock_transfer_items (company_id, transfer_id, product_id, quantity) VALUES ($1, $2, $3, $4)`, [companyId, rows[0].id, product_id, quantity] ); } return successResponse(rows[0], 201); } catch (e: any) { return errorResponse(e.message, 500); }
}
