import { query } from '@/api/lib/db';
import { getCompanyId, successResponse, errorResponse } from '@/api/lib/helpers';
import { NextRequest } from 'next/server'; export async function GET(request: NextRequest) { try { const companyId = await getCompanyId(request); if (!companyId) return errorResponse('Company ID not found', 400); const { rows } = await query( `SELECT * FROM notification_preferences WHERE company_id = $1 LIMIT 1`, [companyId] ); if (rows.length === 0) { return successResponse({ email_enabled: false, email_address: '', task_deadline: true, milestone_deadline: true, project_deadline: true, timesheet_reminders: true, }); } return successResponse(rows[0]); } catch { return errorResponse('Failed to fetch notification preferences', 500); }
} export async function PUT(request: NextRequest) { try { const companyId = await getCompanyId(request); if (!companyId) return errorResponse('Company ID not found', 400); const body = await request.json(); const { email_enabled, email_address, task_deadline, milestone_deadline, project_deadline, timesheet_reminders } = body; const existing = await query('SELECT id FROM notification_preferences WHERE company_id = $1', [companyId]); if (existing.rows.length === 0) { const { rows } = await query( `INSERT INTO notification_preferences (company_id, user_id, email_enabled, email_address, task_deadline, milestone_deadline, project_deadline, timesheet_reminders) VALUES ($1, (SELECT id FROM profiles WHERE company_id = $1 LIMIT 1), $2, $3, $4, $5, $6, $7) RETURNING *`, [companyId, email_enabled, email_address, task_deadline, milestone_deadline, project_deadline, timesheet_reminders] ); return successResponse(rows[0]); } const { rows } = await query( `UPDATE notification_preferences SET email_enabled = $1, email_address = $2, task_deadline = $3, milestone_deadline = $4, project_deadline = $5, timesheet_reminders = $6 WHERE company_id = $7 RETURNING *`, [email_enabled, email_address, task_deadline, milestone_deadline, project_deadline, timesheet_reminders, companyId] ); return successResponse(rows[0]); } catch { return errorResponse('Failed to update notification preferences', 500); }
}
