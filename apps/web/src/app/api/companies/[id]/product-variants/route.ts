import { query } from '@/api/lib/db';
import { getCompanyId, successResponse, errorResponse, parseSearchParams, paginatedResponse } from '@/api/lib/helpers';
import { NextRequest } from 'next/server'; export async function GET(request: NextRequest) { try { const companyId = await getCompanyId(request); if (!companyId) return errorResponse('Company ID not found', 400); const { page, limit, offset } = parseSearchParams(request); const url = new URL(request.url); const productId = url.searchParams.get('product_id'); let whereClause = 'WHERE pv.company_id = $1'; const params: any[] = [companyId]; let paramIndex = 2; if (productId) { whereClause += ` AND pv.product_id = $${paramIndex}`; params.push(productId); paramIndex++; } const countResult = await query(`SELECT COUNT(*) FROM product_variants pv ${whereClause}`, params); const dataResult = await query( `SELECT pv.*, json_build_object('id', p.id, 'name', p.name, 'sku', p.sku) as product FROM product_variants pv JOIN products p ON pv.product_id = p.id ${whereClause} ORDER BY pv.created_at DESC LIMIT $${paramIndex} OFFSET $${paramIndex + 1}`, [...params, limit, offset] ); return paginatedResponse(dataResult.rows, parseInt(countResult.rows[0].count), page, limit); } catch (err) { console.error('Product variants error:', err); return errorResponse('Internal server error', 500); }
} export async function POST(request: NextRequest) { try { const companyId = await getCompanyId(request); if (!companyId) return errorResponse('Company ID not found', 400); const body = await request.json(); const { product_id, sku, name, attributes, cost_price, sale_price, barcode } = body; if (!product_id || !sku) return errorResponse('product_id and sku are required', 400); const result = await query( `INSERT INTO product_variants (company_id, product_id, sku, name, attributes, cost_price, sale_price, barcode) VALUES ($1, $2, $3, $4, $5, $6, $7, $8) RETURNING *`, [companyId, product_id, sku, name || null, attributes ? JSON.stringify(attributes) : '{}', cost_price || null, sale_price || null, barcode || null] ); return successResponse(result.rows[0], 201); } catch (err) { console.error('Create variant error:', err); return errorResponse('Internal server error', 500); }
}
