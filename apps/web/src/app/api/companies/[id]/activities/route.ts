import { query } from '@/api/lib/db';
import { getCompanyId, successResponse, errorResponse, parseSearchParams, paginatedResponse } from '@/api/lib/helpers';
import { NextRequest } from 'next/server'; export async function GET(request: NextRequest) { try { const companyId = await getCompanyId(request); if (!companyId) return errorResponse('Company ID not found', 400); const { page, limit, search, sort: requestedSort, order, offset } = parseSearchParams(request); const { searchParams } = new URL(request.url); const type = searchParams.get('type'); const relatedType = searchParams.get('related_type'); const relatedId = searchParams.get('related_id'); let where = 'WHERE a.company_id = $1'; const params: any[] = [companyId]; let paramIndex = 2; if (type && type !== 'all') { where += ` AND a.type = $${paramIndex}`; params.push(type); paramIndex++; } if (relatedType) { where += ` AND a.related_type = $${paramIndex}`; params.push(relatedType); paramIndex++; } if (relatedId) { where += ` AND a.related_id = $${paramIndex}`; params.push(relatedId); paramIndex++; } if (search) { where += ` AND a.subject ILIKE $${paramIndex}`; params.push(`%${search}%`); paramIndex++; } const allowedSort = ['due_date', 'created_at', 'type', 'subject']; const sortColumn = allowedSort.includes(requestedSort) ? requestedSort : 'created_at'; const { rows } = await query(` SELECT a.*, COALESCE(p.first_name || ' ' || p.last_name, 'Sin asignar') as assigned_name FROM activities a LEFT JOIN profiles p ON a.assigned_to = p.id ${where} ORDER BY a.${sortColumn} ${order === 'desc' ? 'DESC' : 'ASC'} OFFSET $${paramIndex} LIMIT $${paramIndex + 1} `, [...params, offset, limit]); const { rows: countRows } = await query(`SELECT COUNT(*) FROM activities a ${where}`, params); const total = parseInt(countRows[0]?.count || '0'); return paginatedResponse(rows, total, page, limit); } catch (err: any) { return errorResponse(err.message, 500); }
} export async function POST(request: NextRequest) { try { const companyId = await getCompanyId(request); if (!companyId) return errorResponse('Company ID not found', 400); const body = await request.json(); const { type, subject, description, related_type, related_id, due_date } = body; if (!type || !subject) return errorResponse('Type and subject are required', 400); const { rows } = await query(` INSERT INTO activities (company_id, type, subject, description, related_type, related_id, due_date) VALUES ($1, $2, $3, $4, $5, $6, $7) RETURNING * `, [companyId, type, subject, description || null, related_type || null, related_id || null, due_date || null]); return successResponse(rows[0], 201); } catch (err: any) { return errorResponse(err.message, 500); }
}
