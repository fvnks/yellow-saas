import { query } from '@/api/lib/db';
import { getCompanyId, successResponse, errorResponse, parseSearchParams, paginatedResponse,
} from '@/api/lib/helpers';
import { NextRequest } from 'next/server'; export async function GET(request: NextRequest) { try { const companyId = await getCompanyId(request); if (!companyId) return errorResponse('Company ID not found', 400); const { page, limit, sort: requestedSort, order, offset } = parseSearchParams(request); const allowedSortColumns = ['created_at', 'period_start', 'status', 'total_amount', 'net_amount']; const sort = allowedSortColumns.includes(requestedSort) ? requestedSort : 'period_start'; const url = new URL(request.url); const status = url.searchParams.get('status'); const params: any[] = [companyId]; let where = 'WHERE company_id = $1'; let paramIndex = 2; if (status) { where += ` AND status = $${paramIndex}`; params.push(status); paramIndex++; } const countResult = await query(`SELECT COUNT(*) as count FROM payroll_runs ${where}`, params); const total = parseInt(countResult.rows[0]?.count || '0'); params.push(offset, limit); const { rows } = await query( `SELECT * FROM payroll_runs ${where} ORDER BY ${sort} ${order === 'asc' ? 'ASC' : 'DESC'} OFFSET $${paramIndex} LIMIT $${paramIndex + 1}`, params ); return paginatedResponse(rows, total, page, limit); } catch { return errorResponse('Internal server error', 500); }
} export async function POST(request: NextRequest) { try { const companyId = await getCompanyId(request); if (!companyId) return errorResponse('Company ID not found', 400); const body = await request.json(); const { period_start, period_end, notes } = body; if (!period_start || !period_end) { return errorResponse('period_start and period_end are required', 400); } // Check for existing run in same period const existing = await query( `SELECT id FROM payroll_runs WHERE company_id = $1 AND period_start = $2 AND period_end = $3`, [companyId, period_start, period_end] ); if (existing.rows.length > 0) { return errorResponse('Ya existe una nomina para este periodo', 409); } const { rows } = await query( `INSERT INTO payroll_runs (company_id, period_start, period_end, period_label, notes, status) VALUES ($1, $2, $3, $4, $5, 'draft') RETURNING *`, [companyId, period_start, period_end, `${period_start.slice(0, 7)}`, notes || null] ); return successResponse(rows[0], 201); } catch { return errorResponse('Internal server error', 500); }
}
