'use client'; import { useEffect, useState } from 'react'; import Link from 'next/link'; import { motion, AnimatePresence } from 'framer-motion'; import { ChevronDown, Ticket, Search, LifeBuoy, BookOpen, MessageSquare } from 'lucide-react'; interface FaqItem { id: string; question: string; answer: string; category: string; } export default function AyudaPage() { const [faqItems, setFaqItems] = useState<FaqItem[]>([]); const [loading, setLoading] = useState(true); const [search, setSearch] = useState(''); const [category, setCategory] = useState('Todos'); const [openId, setOpenId] = useState<string | null>(null); useEffect(() => { fetch('/api/support/faq') .then(res => res.json()) .then(data => { if (data.success) setFaqItems(data.data || []); }) .catch(err => console.error('Failed to load FAQ:', err)) .finally(() => setLoading(false)); }, []); const categories = ['Todos', ...Array.from(new Set(faqItems.map(i => i.category)))]; const filtered = faqItems.filter(item => { const matchCategory = category === 'Todos' || item.category === category; const matchSearch = search === '' || item.question.toLowerCase().includes(search.toLowerCase()) || item.answer.toLowerCase().includes(search.toLowerCase()); return matchCategory && matchSearch; }); return ( <div className="space-y-6"> <div className="flex items-center justify-between"> <div> <h1 className="text-xl font-bold text-foreground">Centro de Ayuda</h1> <p className="text-sm text-muted-foreground mt-1">Encuentra respuestas a tus preguntas frecuentes</p> </div> <Link href="/ayuda/tickets" className="bg-primary hover:bg-primary/90 text-white px-4 py-2 rounded-lg text-sm font-medium flex items-center gap-2 transition-all duration-150 active:scale-[0.98]"> <Ticket className="w-4 h-4" /> Crear Ticket </Link> </div> <div className="bg-card border border-border rounded-xl shadow-sm p-6"> <div className="flex flex-col items-center text-center mb-6"> <div className="w-14 h-14 bg-muted rounded-xl flex items-center justify-center mb-4"> <LifeBuoy className="w-7 h-7 text-foreground" /> </div> <h2 className="text-lg font-bold text-foreground">¿En qué podemos ayudarte?</h2> <p className="text-sm text-muted-foreground mt-1 max-w-lg">Busca entre nuestras preguntas frecuentes o crea un ticket de soporte</p> <div className="relative w-full max-w-lg mt-4"> <Search className="absolute left-3 top-1/2 -translate-y-1/2 w-4 h-4 text-muted-foreground" /> <input type="text" value={search} onChange={e => setSearch(e.target.value)} placeholder="Buscar una pregunta..." className="w-full bg-muted border border-border rounded-lg pl-10 pr-4 py-2.5 text-sm text-foreground placeholder-muted-foreground focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition-colors" /> </div> </div> <div className="flex flex-wrap justify-center gap-2"> {categories.map(cat => ( <button key={cat} onClick={() => setCategory(cat)} className={`px-3 py-1.5 rounded-full text-xs font-medium transition-colors ${ category === cat ? 'bg-primary text-white' : 'bg-muted text-foreground hover:bg-muted' }`} > {cat} </button> ))} </div> </div> <div className="bg-card border border-border rounded-xl shadow-sm overflow-hidden"> {loading ? ( <div className="divide-y divide-slate-100"> {[1, 2, 3, 4].map(i => ( <div key={i} className="px-6 py-5"> <div className="h-4 w-1/3 bg-muted rounded animate-pulse" /> </div> ))} </div> ) : filtered.length === 0 ? ( <div className="p-12 text-center"> <BookOpen className="w-10 h-10 text-foreground mx-auto mb-3" /> <p className="text-sm text-muted-foreground">No se encontraron resultados</p> <p className="text-xs text-muted-foreground mt-1">Prueba con otra búsqueda o crea un ticket de soporte</p> <Link href="/ayuda/tickets" className="inline-flex items-center gap-2 mt-4 bg-primary hover:bg-primary/90 text-white px-4 py-2 rounded-lg text-sm font-medium transition-all duration-150 active:scale-[0.98]"> <MessageSquare className="w-4 h-4" /> Crear Ticket </Link> </div> ) : ( filtered.map((item, index) => { const isOpen = openId === item.id; return ( <div key={item.id || index} className="border-b border-border last:border-b-0"> <button onClick={() => setOpenId(isOpen ? null : item.id)} className={`w-full flex items-center justify-between px-6 py-4 text-left transition-colors ${ isOpen ? 'bg-muted/80' : 'hover:bg-muted' }`} > <div className="flex items-center gap-3"> <span className={`inline-flex items-center px-2 py-0.5 rounded-full text-[9px] font-semibold border transition-colors ${ isOpen ? 'bg-blue-50 text-blue-700 border-blue-200' : 'bg-muted text-foreground border-border' }`}> {item.category} </span> <span className={`text-sm font-medium transition-colors ${isOpen ? 'text-blue-700' : 'text-foreground'}`}>{item.question}</span> </div> <motion.span animate={{ rotate: isOpen ? 180 : 0 }} transition={{ duration: 0.3, ease: 'easeInOut' }} className="flex-shrink-0" > <ChevronDown className={`w-4 h-4 ${isOpen ? 'text-blue-600' : 'text-muted-foreground'}`} /> </motion.span> </button> <AnimatePresence initial={false}> {isOpen && ( <motion.div initial={{ height: 0, opacity: 0 }} animate={{ height: 'auto', opacity: 1 }} exit={{ height: 0, opacity: 0 }} transition={{ duration: 0.35, ease: [0.4, 0, 0.2, 1] }} className="overflow-hidden" > <div className="px-6 pb-4 pl-16"> <p className="text-sm text-foreground leading-relaxed">{item.answer}</p> </div> </motion.div> )} </AnimatePresence> </div> ); }) )} </div> </div> ); } 