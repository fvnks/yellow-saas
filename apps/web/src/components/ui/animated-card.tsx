import { cn } from '@/lib/utils'; import { ReactNode } from 'react'; interface AnimatedCardProps { children: ReactNode; className?: string; hover?: boolean; delay?: number; } export function AnimatedCard({ children, className, hover = true, delay = 0 }: AnimatedCardProps) { return ( <div className={cn( 'border border-border rounded-xl shadow-sm bg-card ', 'animate-fade-in-up', hover && 'hover:shadow-md hover:border-border transition-all duration-300', className )} style={{ animationDelay: `${delay}ms` }} > {children} </div> ); } 